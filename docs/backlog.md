# NetworkHealthCheck backlog — what is known and not yet done

This page is the project's working list: the items that are open, what would close each of them, and the numbers of the ones already closed. It was the last section of `healthcheck/VALIDATION.md` until 2026-09-08, under a heading that still said *backlog for v1.1.1*, grouped by the review that raised each item rather than by number and with the closed ones struck through in place — so the question "what is still open?" was answered by reading a thousand-line record to its end and looking for the lines without strike-through. The record keeps what it is for: what was run, on which machine, what it measured and what shipped. This page keeps what is left.

It is in the repository and not in the package, like [`application-control.md`](application-control.md) beside it, because it changes between releases while a shipped document may not; it carries no version of its own, and the in-package IT deployment manual links to it by the tag of its release.

## How to read an item

- **The number is permanent.** Items are numbered in the order they were raised and are never renumbered or reused; a closed item keeps its number. Every `backlog #N` in the validation record, the manuals, the technical guides, `docs/` and `tests/` means the number on this page. The order below is numeric, which is *not* the order the items were raised in — the review or the run that produced each one is named in its own origin note instead, which is what the old grouping carried.
- **Acceptance.** Most items end with a sentence naming what would have to be true for the item to be closed. An item without one is a decision that has not been taken, not a task waiting for a machine.
- **Kind** says where the work would land: **tool** — the two shipped scripts or the six launchers, so a release; **documents** — the manuals and guides that ship inside the package, so a release too, where the code is right and the account of it is not; **tests** — the validation chain, its self-tests or the campaign driver, which ship nothing; **build** — the asset builder; **measurement** — a machine this project has not had; **decision** — a cost or a policy, where the missing thing is a choice rather than a change.
- **The item text is the statement**, moved here word for word from the record. It was written beside the entry of the run that raised it, so where it says "this machine" or "the entry above" the origin note says which machine and which entry. Where an item has moved on since it was written without being closed, the change is a **Status** line dated at its end, so that the original wording stays readable beside it.

## Open items

| # | | Kind |
|---|---|---|
| 21 | The asset build is not reproducible byte for byte | build |
| 22 | The two shipped scripts are not signed | decision |
| 25 | Two campaign refinements: `M8`'s refusal message, and the bundle name's resolution | tests |
| 29 | The campaign asks a person to type what a script could do | tests |
| 30 | A campaign run from a CI artifact cannot measure `M2` | tests |
| 31 | AppLocker enforcement has never taken effect on a measured machine | measurement |
| 33 | Tiers 2 and 3 of the document-fact step | tests |
| 37 | When the launcher itself is refused, nothing of ours writes anything | decision / measurement |
| 38 | The retransmission counter read fails intermittently, and takes the verdict and the run's length with it | tool |
| 41 | Nothing checks that the package's own file table is complete | tests |
| 42 | Nothing proves the tool changes nothing | tests |
| 43 | The tool does not say when the reports are being written into a synced folder | tool |
| 48 | The walk asks a person to capture what a script could capture | tests |
| 50 | The zh-TW console launcher's last line reaches the screen as a command | tool / measurement |
| 53 | The campaign self-test keeps no record of what the driver did | tests |
| 55 | Nothing checks that this page's index matches its own items | tests |

The table is an index; each item's own paragraph below is the statement.

### 21 — The asset build is not reproducible byte for byte

The asset build is not reproducible byte for byte: `build_asset.py` stamps every ZIP entry with the build time (`zipfile.writestr` with a name takes the current local time), so the same tracked files give a different SHA256 on every build — 223 590 bytes both on the reference machine and on the runner, digests `38056e15…` and `c7d52700…`. With the entries stamped from the commit date instead and the deflate settings unchanged, the digest would identify the content: the asset CI built for the tagged commit could be compared byte for byte with the one released, and anyone could verify the release notes' digest from the tag. Acceptance: two builds of the same commit on the reference machine yield one SHA256, the runner's build of that commit yields the same one (or the difference is explained by the runner's zlib and recorded), and the validator inside the extracted ZIP still passes. — *raised by the first run on GitHub Actions, 2026-09-05.*

**Status, 2026-09-10 — the build clock is isolated from the runner's zlib, and `windows-latest` turns out not to be one machine.** Four CI builds inside eighty minutes, of a package whose content did not change: run-299 on `531b9b2`, run-300 on `4d50150`, run-301 on `af5824b` and run-302 on `4349481`, the three later commits being PR #42's, which touch `docs/backlog.md` and `README.md` and nothing under `healthcheck/` (`git diff 531b9b2..4349481 -- healthcheck/` is empty). All four produced **484 148 bytes** and four different digests: `b4fdc1d4…`, `3df7eb87…`, `afbf10a9…`, `6cf41401…`. **Two pairs of them need no inference at all.** `windows-latest` served two runner image versions inside that hour — `20260907.229.1` for run-299 and run-302, `20260824.214.3` for run-300 and run-301 — so each version built the same content twice, with the same interpreter (`actions/setup-python@v5` resolved the workflow's `3.x` to CPython 3.14.7 in all four runs), and gave a different digest each time. The parenthesis in the acceptance sentence above — *or the difference is explained by the runner's zlib* — accounts for neither pair, and what is left is the entry timestamps this item names. **The second fact is about that parenthesis itself:** the runner is not one machine, so a difference explained away rather than removed has to name the image version as well as the zlib. Nothing else about this item changes, the fix it asks for included. — *measured by PR #42's CI runs, which were built for something else; the fourth is the run of the commit that recorded the first three.*

### 22 — The two shipped scripts are not signed

The two shipped scripts are not signed. The launchers pass `-ExecutionPolicy Bypass` at process scope, and a Group Policy execution policy (`MachinePolicy` / `UserPolicy`) overrides that scope: under `AllSigned` the script does not start, and what the user sees is PowerShell's own "not digitally signed" message through the launcher's pause — the language-mode guard of #18 is inside the script and cannot reach that case (checklist item M8 measures it). An Authenticode signature would satisfy `AllSigned`, let WDAC / AppLocker allow the tool by publisher instead of by a hash per version (`SHA256SUMS.txt` is what an IT department has today), and give SmartScreen something to build a reputation on. An enterprise deployment can sign with an internal CA certificate; general distribution needs a public code-signing certificate, which is a cost and a decision, not a fix. Acceptance: the signed scripts pass the chain unchanged (the packaged manifest is regenerated after signing, since a signature block changes the file), M8 under `AllSigned` runs, and `Get-AuthenticodeSignature` reports `Valid` on a clean machine that trusts the chain. — *raised by the enterprise-readiness review, 2026-09-05.*

### 25 — Two campaign refinements: `M8`'s refusal message, and the bundle name's resolution

Two refinements of the acceptance campaign's round-15 fixes, accepted in round 16 and deferred when the loop was closed: `M8` should require the signature-specific refusal message (`is not digitally signed` / `未經數位簽署`, plus each further display language the campaign meets) rather than PowerShell's generic `SecurityError` / `UnauthorizedAccess` classification, which an AppLocker or Software Restriction Policy block emits as well, and re-read `MachinePolicy` after the run so that the refusal is tied to the policy in force; and the campaign bundle's name should carry milliseconds and the process id, so that two invocations within one second do not collide on `nhc-campaign_<computer>_<campaign>_<time>.zip` and record a bundle failure. Acceptance: a self-test case for each (a captured output with the generic classification only fails `M8`; two bundles written in the same second both exist), the `campaign` step green. — **The bundle half stopped being hypothetical on 2026-09-06.** The `campaign` step failed on GitHub Actions (run 34011079895, PR #17, a documentation-only change that touches nothing the step reads): 124 passed, 1 failed, the failure being case 20's `exit code 0 -> exit code 1` while the scenario assertion beside it passed — the shape a recorded bundle failure produces, since a skip counts nothing and only a failure on record can raise that exit code. What separates the runs is speed, not code: the failing run finished the step in **60.8 s**, and the same self-test passed at **101.5 s** on the runner (the next commit) and at **106.3 s** on the reference machine, both 125 / 125. Cases 19 and 20 invoke the campaign back to back on the same state directory, and each invocation ends by rebuilding `nhc-campaign_<computer>_<campaign>_<time>.zip`; on a runner fast enough to put both in one second the second collides. Not proven — no bundle row was captured from the failing run — but the fix this item already names (milliseconds and the process id in the name) is what would settle it, and until then the `campaign` step is known to be flaky on a fast machine. — *raised by the PR #11 review loop, 2026-09-05.*

**Status, 2026-09-09 — it happened again, and the timing now cuts against the explanation above.** The `campaign` step failed the same way on GitHub Actions run 236 (`34328879764`, commit `1c09b71` of PR #37, documents only again): 124 passed, 1 failed, case 20's `exit code 0 -> exit code 1` with the scenario assertion beside it passing. **The step took 115.8 s** — slower than both runs the note above cites as passing (101.5 s on the runner, 106.3 s on the reference machine) and nearly twice the 60.8 s of the failing run it was drawn from. The collision explanation turns on a runner fast enough to put two invocations in the same second, and the slowest of the four runs is now one of the two that failed. **That does not refute it** — the step's total duration is not the gap between cases 19 and 20, and a slow run can still hold a fast pair — but the timing no longer supports it either, and it was the only evidence the explanation had. **Two further candidates fit the same shape**: `M2` counted `PENDING` at exit while the record said `SKIPPED`, which would break the invariant PR #11 round 14 exists to hold; and the driver ending before its own `exit` line, since `$ErrorActionPreference` is `Stop` and `Save-State` at `:1183` and the first `Write-CampaignSummary` at `:1231` run outside every `try` — PowerShell then exits 1 by itself, with no row added and the state already correct on disk. (The `Save-State` at `:1175` cannot be it: it runs before the scenario loop, so a death there would have left the crafted `M2 = PENDING` on disk and failed the assertion that passed.) **Nothing separates the three, and nothing will, until [#53](#53--the-campaign-self-test-keeps-no-record-of-what-the-driver-did-so-its-failures-cannot-be-diagnosed) makes the driver's own output survive a failing run** — which is why the second occurrence taught nothing the first had not. **Status, 2026-09-09 (later the same day) — #53's harness half shipped, so the next occurrence is readable.** `campaign.log` now carries every invocation's driver output. What to look for when the step next fails: a `bundle not written:` line naming the campaign ZIP confirms the collision this item describes; the same line naming another file points at something else holding a handle; **no** `bundle not written:` line at all, with the driver's output stopping mid-invocation, means the script ended before its own `exit` and the answer is the third candidate rather than either of the first two.

### 29 — The campaign asks a person to type what a script could do

The campaign asks a person to type what a script could do, and the typing is where the run goes wrong. Of the eleven scenarios, four are `reconfigure`, and three of those four change nothing but software configuration: `M7` is one registry value, `M8` is two registry values (or the same setting through `gpedit.msc`), and `M9` is an AppLocker policy plus a service — `Set-AppLockerPolicy -XmlPolicy` writes exactly what `secpol.msc` writes, so the whole console sequence (create the default rules, delete the one for `BUILTIN\Administrators`, tick rule enforcement, start the service, `gpupdate`) is one XML document and two service calls, and the same again reversed. `A2` needs an interactive standard-user desktop, which a script cannot conjure, but the account it needs (`net user`) is one line. Measured cost of leaving this manual: on this machine `M9` alone took 31 minutes of a 105-minute campaign, produced a literally-typed `<auto|demand|disabled>` placeholder, and ended in a revert that `sc config` refused. What must stay with the person is what only a person can do — the VM's network and display settings (`A3`, `A4`, `M4`), the double-click inside the compressed-folder view (`M1`), and the judgement calls (`Windows showed: 1`, `Open Report: shown`, `panel usable`). Two things to settle in the design rather than in passing: the campaign runs unelevated on purpose, because `A1` measures what an ordinary user gets, so an automated policy change needs an elevated helper rather than an elevated campaign; and `M8` through the registry is not the same evidence as `M8` through a real Group Policy, so where `gpedit.msc` exists the GPO path should stay available and the record must keep naming which path a run took (it already records `PolicyWay`). Acceptance: `M7`, `M8` and `M9` change and revert themselves with no console step, their reverts verified as now, the `PolicyWay` fact still recorded, `RECOVER.txt` still written for the case where an automated revert cannot finish, the self-test covering the automated path for each, and a campaign on a machine with none of these editions still refusing what it cannot measure. Raised by the owner after this campaign: "人為操作盡量限制在必要的動作上". — *raised by the second campaign, on the Windows 10 Pro VM, 2026-09-06; "this machine" is that VM.*

### 30 — A campaign run from a CI artifact cannot measure `M2`

A campaign run from a CI artifact cannot measure `M2`. The asset that `main` builds arrives as `validation-chain-<run>.<attempt>.zip`, and neither that ZIP nor the package ZIP inside it carries a Mark of the Web unless the artifact itself was downloaded by a browser on the machine under test and unpacked with Explorer. On this machine the artifact came from the host, so `M2` was skipped for its missing prerequisite and only passed after the mark was written by hand. The prerequisite message already says to download it with the browser of this machine; it could also name the one-line way to write the mark deliberately, and the record should then say the mark was applied rather than downloaded — which is what the entry above does by hand today. Acceptance: the message names both routes, the campaign records which one this run took, and the summary carries it. — *raised by the second campaign, on the Windows 10 Pro VM, 2026-09-06; "this machine" is that VM and "the entry above" its entry in the validation record.*

### 31 — AppLocker enforcement has never taken effect on a measured machine

AppLocker enforcement has never taken effect on any machine this project has measured, and one machine says it should have. On the Windows 10 Pro VM of `win10-zhTW` the effective policy had Script rules `Enabled` with two rules, `AppIDSvc` was running, `gpupdate /force` had been run and `Test-AppLockerPolicy` answered `DeniedByDefault` for the account on the exact file — and the launcher ran that file to completion in `FullLanguage`. Microsoft's "Requirements to use AppLocker" says the edition stopped mattering for enforcement in Windows 10 2004 and newer with [KB 5024351](https://support.microsoft.com/help/5024351), and this machine is 22H2 build 19045.3803, so the edition is not the explanation. What is left to check: whether that servicing update is actually present on the image, whether a locally-authored policy (`secpol.msc`, no GPO and no MDM) is deployed the way the KB covers, whether enforcement needs a reboot rather than a service start, and whether the AppID driver was filtering at all. Until one machine is made to enforce, `M9` cannot pass anywhere and the tool's behaviour under application control is asserted only by `M7`'s language-mode guard, which is a different mechanism. Acceptance: one machine where a denied script is actually refused, `M9` PASS there, and the record naming what made the difference. — *found by the second campaign, 2026-09-06; the first reading of it as an edition limitation was wrong (Codex round 1 on PR #16). What has been measured mechanism by mechanism is on [`application-control.md`](application-control.md).*

### 33 — Tiers 2 and 3 of the document-fact step

Nothing checks that the documents still describe the tool. `sop/` now holds three documents making specific, checkable claims about the shipped code — the SOP's tool section, the support-engineer field manual, the report template — and `healthcheck/` will hold two more when the user manual and the IT deployment manual of #32's release go inside the package. The validation chain does not read `sop/` at all, and the packaged validator checks one string per script (`$script:ToolVersion`, `validate_release.py:70`); no documented fact is tied to the code that makes it true. Three ways this has already gone wrong, every one caught by a review round rather than by a test: a fact corrected in the manual was copied into the template in its **pre-correction** form (an added target that fails is `INFO`, except a DNS name that will not resolve, which is `WARN` and moves the verdict); the same item had to be corrected in **three** places (raw command output and the switch and AP status pages — the manual's coverage table, then its prose, then the template's checklist); and a fix reintroduced something an earlier document already had right (the launcher's `%TEMP%` fallback carries a different file name, `NetworkHealthCheck_LauncherError.txt`). The fourth way is the one no review round can catch, because a review only runs when somebody touches the document: **the code changes and every document goes quietly stale.** Acceptance: a chain step that asserts a small set of load-bearing facts twice — once against the source that makes them true, once against each document that states them — and names the document to update when the source moves. Starting candidates: the language-mode guard's exit code 3; the launcher error file's two names and locations; `TracerouteHops` defaulting to 3; `DHCP: Unknown` as the default of the adapter's mode field; the required/optional status chain for ping targets, including that it stops at the first match; and the CIM fallback hard-coding an adapter as `Up`. Assert on the fact token rather than on a sentence, so that rewording a document does not fail the step. What such a step cannot do is judge a document's *reasoning*: "the link is up and the adapter is healthy" was a wrong inference from correct measurements, and only a review round found it. Best done when the two manuals go inside the package, since the validator's version check has to be extended at the same time. — *raised by the owner while the report template was in review, 2026-09-06: "有可能會出現不一致的問題嗎？"*

**Status, 2026-09-07.** Tier 1 is done and closed: `tests/doc_facts.ps1` with `tests/selftest_docfacts.ps1`, the chain's `docfacts` step, 96 assertions and 40 mutations — the identifiers the documents quote (result tags, fingerprint keys, configuration keys, exit codes, file names, local links, section numbers) against the identifiers the two scripts and the two shipped configuration files define. What it reads is code spans only, never prose. Two tiers remain, as its entry in the validation record says: **tier 2**, the manuals' quoted report strings against a real report of that language, which the chain's `acceptance` step already produces; and **tier 3**, the fingerprint chain's rule order against the field manual's table.

### 37 — When the launcher itself is refused, nothing of ours writes anything

Every refusal this project has measured happens after `Start-NetworkCheck.cmd` has started: PowerShell refuses the script under an `AllSigned` policy (`M8`), or runs it in `ConstrainedLanguage` and the guard of #18 writes its environment report (`M7`). Since 1.2.3 the launcher turns each of those into a suggested action in `LauncherError_<stamp>.txt` (a stamped name since 1.2.14) and on the screen — the ZIP for a missing program file, the machine for a missing PowerShell, the environment report for exit code 3, and for any other non-zero exit the message PowerShell printed above it, with "ask IT to allow `NetworkHealthCheck.ps1`" when that message names a signature or a policy (#28; `tests/launcher_check.ps1` runs the six launchers through every reason, 114 assertions). The case that leaves the person with nothing is the one where the launcher is what is refused: AppLocker's script rules cover `.cmd` as well as `.ps1`, and a product that blocks the `.cmd` file itself — or `cmd.exe` — stops the package before its first line, so Windows shows its own message, no file of ours is written, and there is nothing to send with the support request. A block on `powershell.exe` is **not** this case: the launcher has already started, its call to `%PS_EXE%` returns a non-zero `ERRORLEVEL`, and the reason branch below it writes `LauncherError_<stamp>.txt` with the suggestion to send it together with `PowerShellMessages_<stamp>.txt`, where the message printed in that window is kept since 1.2.14 — which is exactly what #28 covers, and what the `exit code 1` case of `tests/launcher_check.ps1` exercises. Nothing inside the package can fix that from inside — what can be decided is what the package carries for it. It has not been measured: no machine with an EDR product has been part of an acceptance run, and #31 says AppLocker enforcement has never taken effect on any machine this project has looked at, so the state itself has never been produced. Acceptance: one machine where the launcher itself is refused, with the record naming what the person saw and where Windows said it; and a decision, recorded either way, between a one-page printed note in the package — the "it will not start" page an IT department can hand out with the ZIP — and the statement that section 8 of the IT deployment manual is where this belongs and the package carries nothing further. — *split out of #23 when that item was closed, 2026-09-08; #23's own question — whether the launcher should say in plain language what IT can do — was answered by #28 in 1.2.3.*

### 38 — The retransmission counter read fails intermittently, and takes the verdict and the run's length with it

`Get-TcpCounterSnapshot` reads `Win32_PerfRawData_Tcpip_TCPv4` and `Win32_PerfRawData_Tcpip_TCPv6` with `Get-CimInstance -OperationTimeoutSec 8`, once before the sample window and once after; a read that fails is collected per protocol, and `Compare-TcpCounters` writes one *Unable to Check* row per failed read. Measured on `win11-enUS` during the 1.2.4 user-manual walk, 2026-09-08: **the first run of the session lost TCPv4** — `Microsoft.Management.Infrastructure.CimException`, *Timed out* — while every connectivity check passed, and the timeout inside the same pass stretched the window TCPv6 actually measured from the configured 8 seconds to **13.3**; a second run minutes later read both protocols and sampled **9.1**. Exactly one row appeared, so one of the two reads timed out, not both. The likely cause is the WMI performance-counter provider starting cold on a machine where nothing has touched performance counters yet — a normal condition, not a fault — and what makes it worth fixing is *which* run pays for it: the first one, which is the run a person makes when they have a problem, and the measurement it loses is the one that would have shown packet-level trouble. Two candidate fixes, independent and cheap: a throwaway query **before** the sample window, so the provider's start-up cost falls outside the measurement and cannot stretch the other protocol's window; and one retry of a read that timed out. Acceptance: on a machine in the state that reproduces this — a run started in a copy extracted moments earlier, better still with the machine under load — both protocols are read, or *Unable to Check* is written only after a further attempt has also failed; each protocol's reported duration is derived from its own stamps, and **a sample that ran longer than the configured length because another read failed says so in the row** rather than printing a figure that contradicts what the person was told — the serial reads make the delay itself unavoidable, so what the fix owes is an honest number with its reason, not a number that pretends nothing happened; unit tests cover the further attempt, the pre-window query and both duration cases; the rest of the chain is unchanged. — *found by the user-manual walk on `win11-enUS`, 2026-09-08; the manual half of the same observation is a 1.2.5 documents fix, since section 9 lists a permission, a policy and a broken performance counter as the causes and not the timeout the tool itself imposes.*

**Status, 2026-09-08 — the walk falsified the hypothesis above, and the item is better for it.** The timeout recurred at 12:47, on the **sixth** run of the day on that machine (09:31, 10:15, 11:22, 11:33, 11:46, 12:47), so a provider that starts cold cannot be the explanation: it was warm five runs earlier. What is established is narrower and worse than the first reading: **the read fails intermittently on a machine where nothing is wrong**, it costs the retransmission measurement, and it takes the verdict to *Test Incomplete* with it (#39). What is not established is the mechanism. Idle time does not predict it either: the six runs of that day were 09:31 (**timeout**), 10:15, 11:22, 11:33, 11:46 and 12:47 (**timeout**), and the gap before the clean 11:22 run was 67 minutes while the gap before the failing 12:47 run was 61 — so a provider host recycled after idle does not separate them, and neither does a boot or a resume, since the machine did neither. One correlation does hold across all six, and is recorded because it was observed rather than because it is proven: **both failures followed an Explorer extraction of the package into a OneDrive-synced folder within minutes** (the first extraction before the 09:31 run; `NHC-blocked` extracted at 12:40, run at 12:47), and none of the four clean runs followed an extraction at all. Thirty-one files landing in a synced folder means the sync client is reading and uploading them, and `Win32_PerfRawData_*` queries are sensitive to disk and CPU load. Two failures out of two, none out of four: a lead, not a mechanism. **Third occurrence, same day, and the condition is now stated better.** The 13:03 run — the first run inside the freshly extracted `NHC-unblocked` copy — lost TCPv4 as well, making it **three failures in three runs that followed an extraction, and none in the four that did not** (09:31 fail, 10:15 clean, 11:22 clean, 11:33 clean, 11:46 clean, 12:47 fail, 13:03 fail). What that sharpens is the condition, and it also narrows my earlier reading: **the failing runs are the first run inside a freshly extracted copy**, which is not the same claim as “OneDrive was busy”. Two mechanisms fit the same evidence and neither is established — a sync client uploading thirty-one new files, and the antivirus scanning thirty-one newly created files and a script executing from a path it has not seen before. Both produce the disk and CPU load that `Win32_PerfRawData_*` is sensitive to; only the second needs no cloud folder at all. So the condition to reproduce is *a run started in a copy extracted moments earlier*, on a synced folder and on a plain local one, which separates the two. **The control arrived seven minutes later and the cost is now measured.** A *Run Again* in the same window, with no extraction before it, read both protocols cleanly — three failures in three runs that followed an extraction, none in five that did not. The two runs are otherwise the same work on the same machine, and they differ like this: the failing run took **25.9 seconds** and reported a sample window of **14.5** against the configured 8; the clean one took **11.7** and reported **8.8**. So the timeout costs the measurement, stretches the other protocol's window by its own eight seconds, and **more than doubles the run** — against a manual that tells the person to expect about ten seconds. That is a third thing the retry buys, beside the measurement and the verdict.

**How it gets verified, decided 2026-09-08 — two levels, and only one of them belongs in the chain.**

*Deterministic, and this is what guards the fix.* Every performance-counter read passes through one named helper, `Get-CimOrWmiInstance` (`NetworkHealthCheck.ps1:2802`), which the unit tests can load by AST the way they load the other helpers. Three cases, no machine state required: a stub that throws a `CimException` “Timed out” on the first call and returns a counter object on the second must produce a normal reading, not an *Unable to Check* row; a stub that throws on both calls must produce exactly today's row, because a retry that swallows a real failure is worse than no retry; and a failure on one protocol must leave the other protocol reporting **its own** interval, taken from its own stamps, together with the sentence that says the sample ran long because another read failed — not an unstretched window, which the serial reads make impossible, but an honest one with its reason. These run in the chain, in both languages, and they cost nothing.

*Statistical, and it must never gate a build.* Whether the failure happens at all is a property of a machine under load, so the harness that measures it — N reads with the machine idle, N with it under deliberate local file I/O (a large tree copied by the harness itself; no sync client, because CI has none) — reports a **failure rate per condition** and is run on demand, not by the chain. A step that fails one run in twenty teaches nothing and erodes trust in every other step: this project already owns one such case, the campaign self-test's bundle-name collision on a fast runner (#25), and recorded it as a measurement rather than hiding it behind a re-run. The harness is how the lead above gets settled and how the fix is shown to move the rate; the three unit cases are how the fix stays fixed.

*Where the retry goes.* `Get-CimOrWmiInstance` is the one place the counter reads pass through, but the operating-system query (`:996`) and the adapter-configuration queries (`:1196`) call `Get-CimInstance` directly with the same eight-second timeout and do not use the helper. A retry inside the helper therefore leaves them out — which is a decision to state in the change, not to discover afterwards: either route them through the helper too, or record why they stay as they are. The consequence for the fix: what the evidence rules out is the **diagnosis**, not the **mechanism**. A throwaway query before the sample window is itself an extra attempt: if load makes one call time out and the next succeed, it can absorb the transient exactly as a retry does, and it has an advantage a retry cannot have — it spends its attempt **outside** the measured window, so it cannot stretch the sample the way the failing call did (14.5 s against a configured 8). What only a retry covers is a call that fails on the measured attempt itself. Both stay in the fix, for those two different reasons. **And the stretch has its own fix, smaller than it looked.** `Get-TcpCounterSnapshot` reads the two protocols serially and stamps each with its own `Timestamp`, but `Compare-TcpCounters` derives the duration from the **enclosing** snapshot's stamps (`NetworkHealthCheck.ps1:2901`), which are taken after both reads — so a timeout in one protocol's read is reported as part of the other protocol's sample window. The deltas are unaffected, because each is the difference between that protocol's own two readings, and the rate is a ratio and not a time; **only the reported duration is wrong**. Computing it from the per-protocol stamps that already exist gets most of the way, **and not all of it**: each stamp is taken *after* that protocol's query returns and TCPv4 is queried first, so a timeout on the ending TCPv4 read still delays the ending TCPv6 read and its stamp. The interval it then reports is TCPv6's **true** window — the sample really did span that long, and the delta really is over that span — so the number is not wrong; what is wrong is showing it without a word while the person was told the sample lasts eight seconds. So the fix has two halves: derive each duration from that protocol's own stamps, and **say when a sample ran long because another read failed**, rather than printing a figure that silently contradicts the configured length. Both have to land before a retry does, since a retry on the ending TCPv4 read would push the TCPv6 stamp further still — **and the retry has to leave a trace when it succeeds**, or the explanation cannot be written at all: today a failed attempt reaches `Compare-TcpCounters` only as an error that made it into the snapshot, so a read that timed out and then succeeded would hand back a clean counter and no record, and the row for the *other* protocol would have nothing to say about why its sample ran long. The snapshot has to carry attempts that failed even where a later one worked. The acceptance above stands, with a machine under deliberate load as the condition worth reproducing, and with both halves kept: the pre-window query for the attempt that costs the measurement nothing, the retry for the failure that lands on the measured call.

**Status, 2026-09-09 — the verdict half of this item is no longer this item's to fix.** The decision recorded under closed item #39 takes the verdict away from quality rows: a counter read that failed still writes its *Unable to Check* row, and is now named in the summary, but it no longer turns a run in which every required check passed into *Test Incomplete*. That is the first half of this item's title. The second half is untouched and is what remains here — the run's length, and the window the other protocol actually measured: eight seconds configured, 13.3 measured, because one read waited out its timeout inside the sample. The retry, the one place it goes, the queries that do not pass through that place, and the per-protocol stamps all stand exactly as written above, and so does the acceptance sentence.

**Status, 2026-09-09 (1.2.8) — everything but the first clause is done and measured; that one waits for a machine.** Four changes, and each answers a sentence of the item above.

**The second attempt.** `Get-CimOrWmiInstance` takes `-Attempts` and a list to record what failed in; the two counter reads of a snapshot ask for **two** and nothing else in the tool asks for more than one. A read that fails twice is not attempted a third time, and its row is the row it always was — same status, same check name, same message, because a retry that hides a real failure is worse than no retry — with the attempts behind it added to the details. No attempt is classified: a read that failed instantly for a permission or a missing class is attempted again too, and costs nothing to attempt, where classifying it would mean reading a message the operating system writes in its own language (#14 is that lesson).

**The read before the window.** `Get-TcpCounterSnapshot -WarmUp`, on the **baseline** snapshot only: one throwaway read per class, in a pass of its own before either counter is read, its reading discarded, its failure neither an error nor a measured attempt. The pass of its own is the point, and it took a review round to get there (PR #40, round 3): interleaved with the measured reads, TCPv6's warm-up fell after TCPv4's baseline stamp and so inside TCPv4's window — and a warm-up that is merely **slow**, which is the start-up cost the feature exists to absorb, would have lengthened that window with no failure behind it to explain the seconds. The ending snapshot takes none — inside the window it would lengthen the very thing it is there to protect. Whether the first read of a session is the one that fails is the question this item leaves open, so the warm-up's failures are recorded (`WarmUpFailures`, kept apart from the measured `FailedAttempts`) and shown in the row of the protocol they belong to.

**The trace, which the retry could not do without.** Every attempt that failed is kept with the seconds it spent, *even where a later attempt succeeded* — the case the item named, where a read that timed out and then worked handed back a clean counter and no record, so nothing could explain the seconds it had spent. A row for a counter that could not be read names the attempts behind it; a row whose window carried a read that **failed** names those seconds and those reads. Nothing else in a window is itemised - a read that was merely slow, or the checks that run between the two samples, lengthen it without a line of their own (PR #40, round 13), which is why the row prints the duration it truly measured rather than an accounting of it. A row that says a counter could not be read carries that protocol's failed reads from **both** snapshots when the other snapshot wrote no row of its own for it, because a protocol without a reading has no quality row and that row is the only other place they are named (PR #40, round 4); each line says which snapshot it belongs to, so a baseline failure can never read as an ending one.

**Three ways the guarantee still leaked, all found in review round 5 and all closed.** A query that returns **nothing**, or an instance without the counter fields, is a read that failed as surely as one that threw — and that check sat outside the retry loop, so a timeout got a second attempt and an empty answer did not. It is inside the attempt now (`-RequireProperty`), which the pre-window read does not ask for, its reading being discarded. The **counter-reset** row returned before the evidence lines were appended, so a read that failed and was redeemed vanished from a run whose counters had reset; every row of a protocol now carries them. And a **baseline where both classes failed** was thrown away by its own step guard: `Invoke-CheckStep` caught the throw, wrote its generic step-error row and returned `$null`, taking four attempts, their seconds and any pre-window failures with it, and leaving the analysis to say only that complete data was unavailable. The snapshot is returned whatever it holds, and one row per read that failed is written out of it. **The acceptance validator had to learn the new shape** (PR #40, round 9): `Test-ResultSet` still expected a step-error row and one generic row on a machine where neither class could be read — the shape 1.2.7 produced — so an end-to-end run on exactly the machine this path exists for would have been rejected as malformed. It now expects one row per read that failed, per sample, and no step error at all; both protocols are named whatever their counters did; and `selftest_resultset.ps1` pins the new shape and rejects the old one (41 → 42 cases). **And the chain's own probe had to learn the retry too** (round 10): `Get-MachineFacts` asked each counter class once, so a transient failure there would have called a class unreadable that the run reads on its second attempt, and the result-set check would have rejected a correct report on exactly the machine this release is for. The probe now mirrors the shipped read — two attempts, the same two fields — and an AST assertion in `unit_tests.ps1` fails if the shipped call stops saying what the probe assumes. **The reset row was refined once more in round 7**: it prints the window it spans, so the note's *these N seconds* has something to refer to, and it no longer borrows the sentence about the deltas above — a row that reports an uncalculable delta has none. That sentence now belongs to the rows that do.

**And the version bump rewrote history until round 7 caught it.** Replacing every `1.2.7` label in the manuals also rewrote three provenance statements — the extra-TCP label's width, the stray positional value, and the summary's *All required checks passed* — which describe what changed in **1.2.7**, not in this release. They cannot simply be restored, because the packaged validator requires every version label in a manual to be the version being built; so they now state the behaviour without a version at all (*in earlier versions*, 「早期版本裡」), which is what a manual that describes the tool as shipped should have said. Six files: both manuals, both formats, both languages, minus the two HTML twins that never carried the sentence.

**The duration, and the sentence beside it.** Each row's sample duration now comes from **that protocol's own two stamps**, and the configured minimum stands beside it — a minimum, which is what `Wait-ForMinimumTcpSample` enforces, not the target the old label implied. A row also says when its window carried a failed read, and the rule for which reads those are took a review round to get right (PR #40, round 1). A window is lengthened by a read that delayed the stamp closing it without delaying the stamp opening it: in the **ending** snapshot, every read at or before this protocol; in the **baseline** snapshot, every read *after* it — a stalled TCPv6 baseline read pushes TCPv6's own opening stamp and the rest of the run alike, but not TCPv4's, so it lands squarely inside TCPv4's window. The first draft called every baseline failure harmless, which is true only of the protocol read first. The pre-window reads are in neither list, because round 3 moved them all in front of both baseline stamps.

**The queries that do not pass through the helper, decided rather than discovered.** `Get-SystemSummary`'s `Win32_OperatingSystem` query and the CIM fallback's `Win32_NetworkAdapterConfiguration` / `Win32_NetworkAdapter` pair still call `Get-CimInstance` directly with the same eight-second limit, and they stay that way: none of them is a `Win32_PerfRawData_*` read, no failure of theirs is on record here or anywhere in this project's runs, each already sits on a path that handles its own failure (the operating-system query reports the system as unknown; the adapter pair *is* the fallback for NetTCPIP), and giving them a second attempt would double their worst case on no evidence at all. If one of them ever fails the way the counters do, this is the paragraph to come back to.

**The cost, stated rather than discovered.** On a machine whose counters answer, the two pre-window reads are the whole addition and they cost milliseconds. On one where every read runs out of time, a run's six reads spend up to **80 seconds** between them against 32 before — the price of not losing the measurement, and the manuals now say a counter read that runs out of time is tried once more before the tool gives up on it.

**What the tests hold.** 93 assertions in each language inside the existing `unit` step (280 × 2), stubbing `Get-CimInstance` itself so that what runs above the stub is the shipped retry loop and the shipped bookkeeping, with the exception the walk recorded (`CimException`, *Timed out*). Two hand-built snapshot fixtures pin the arithmetic: the failure the walk found — TCPv4's two timeouts inside TCPv6's window — makes the row say **24.5** seconds, its own, and not the 26 the enclosing snapshots span, which is the number nobody could explain; a clean pair says nothing about attempts at all. **Mutation-tested seventeen ways, every one caught**: the duration back on the enclosing stamps (3 assertions fail), no second attempt (8), no pre-window read (2, both on the AST — the call site is the only place that can be asserted without a machine), the window note not written (2), an attempt that a later one redeems not recorded (9), the unreadable-counter row losing its attempts (1), the warm-up taking two attempts of its own (2); for round 1's rule, the pre-fix reckoning that counted no baseline failure against any window (3), a protocol's own baseline failures counted against its own window (2), the pre-window reads of later protocols left out (3), and a pre-window read not marked as one where it is quoted (1); for round 3's, one protocol left unwarmed (2) and a pre-window read counted against another protocol's window again (4); and for round 4's, the other snapshot's attempts appended unconditionally (2), never appended (4), an ending line labelled as the baseline's (2) and a borrowed line labelled with the borrowing row's snapshot (1). **The last two exist because a mutation of mine survived**: nothing asserted which snapshot a line belonged to until it did. **Three trees of the review are measurements of their own**: run against the tree as it stood before round 3, before round 4 and before round 5, the suite fails with exactly the 5, 4 and 8 assertions that describe those defects and nothing else. **The review's own trees are the fourteenth and fifteenth measurements**: run against the tree as it stood before round 3, the suite fails with exactly the five assertions that describe that defect — the warm-up call order (TCPv4, TCPv4, TCPv6, TCPv6, interleaved) and the four about a warm-up counted inside another protocol's window.

**What the harness measured, and what it did not.** `tests\tcp_counter_rate.ps1` is the statistical level the decision above asked for: N snapshots per condition, idle and under deliberate local file I/O it generates itself, reporting first-attempt failures, reads lost after both attempts, and the seconds spent on failures — on demand, never in the chain. On the reference machine (`LULALAT14`, Windows 11 10.0.26200) the campaigns of 2026-09-09 produced **0 first-attempt failures in 160 reads**: 50 idle, and 110 under load with every worker verified copying before the first sample — 3 of 3 after 0.4 s over 400 × 256 KB, and 6 of 6 after 1.5 s over 800 × 512 KB. **The load halves were measured twice**, because round 14 of the review found the harness starting its jobs and sampling without waiting for any of them to reach a first copy: `Start-Job` spawns a child PowerShell, so some of the original load samples may have been idle samples wearing the load label. The figures above are the re-measured ones; the first set is superseded rather than quoted. So the failure does not reproduce here, which is the honest reading and the reason the harness exists rather than a claim that the fix works: the machine that showed it was `win11-enUS`, three times, each on the first run inside a freshly extracted copy.

**What is left is the first clause**, and it cannot be forced from here: on a machine in the state that reproduces this, both protocols are read, or *Unable to Check* is written only after a further attempt has also failed. The harness is how that gets answered on the machine that has the condition — the next campaign on `win11-enUS`, where a run inside a freshly extracted copy is the campaign's own first step. Until then this item stays open on a clause that waits for a machine, like [#48](#48--the-walk-asks-a-person-to-capture-what-a-script-could-capture)'s and [#53](#53--the-campaign-self-test-keeps-no-record-of-what-the-driver-did-so-its-failures-cannot-be-diagnosed)'s.

**Status, 2026-09-13 — one first-attempt failure on the reference machine, on the first read of a fresh process.** `tests\tcp_counter_rate.ps1 -Iterations 30`, run for backlog #65's cost measurement: idle, 60 reads, **one first-attempt failure — iteration 1, TCPv4, attempt 1 timed out after 8.3 s, the second attempt succeeded** — and it was the first counter read the process made, the read the tool's own pre-window pass takes outside the window; under load, 60 reads, none. With the 2026-09-09 campaigns the reference machine stands at 1 first-attempt failure in 280 reads and 0 lost. It is one observation, not the mechanism: a first read costing its whole limit fits the pre-window read's purpose and says nothing about the runs on `win11-enUS` that failed five reads into a session. The same script now also prints how long the reads that succeeded took, per condition, which is what #65 needed and the reason it was run.


### 41 — Nothing checks that the package's own file table is complete

`tests/doc_facts.ps1` checks one direction: every launcher or program file name a document quotes has to be in the package (check E1). The reverse is unchecked — a file that ships and that no document names goes unnoticed, and the document where that matters is the user manual's section 8, *The files in the package*, which walks the reader through the package file by file. 1.2.4 is the release that shows the risk rather than a hypothetical one: two manuals joined the package and both `README_*.txt` left it, and what kept section 8 right was a person editing four files by hand. The walk checks it by hand in both directions (rows W36 and W37) and it is the kind of check a person should never have to make twice. Acceptance: a chain check that every file shipped at the package root and in each language folder is named by the user manual's file table, in both languages and both formats, with a named waiver list for what the table deliberately groups rather than enumerates (`docs\`, `tools\`, the `Reports\` folder the tool creates on first run) and a reason recorded for each waiver; a new shipped file that no table names fails the step; the negative self-test covers a missing name and a stale name, as `selftest_docfacts.ps1` does for the checks it already has. — *raised while asking which rows of the user-manual walk could be automated, 2026-09-08; it belongs beside backlog #33's tiers rather than inside them, because it reads the package listing rather than the scripts.*

### 42 — Nothing proves the tool changes nothing

"It changes nothing" is the tool's first promise to the person who runs it — section 1 of the user manual, the first answer of its FAQ, the opening of the IT deployment manual — and no test measures it. The chain runs the script and reads what it wrote; no step compares the machine before the run with the machine after. A regression that started writing a registry value, or a cmdlet call that flipped an adapter setting on the way to reading it, would be caught by a person noticing, or by nobody. The measurement is cheap and deterministic, which is what makes the gap worth closing: take `ipconfig /all`, `route print`, `netsh winhttp show proxy`, the WinINET proxy key under `HKCU`, `netsh advfirewall show allprofiles` and `Get-NetAdapter` before and after a console run, and compare. **The measurement has to subtract the machine, not ignore it.** A before-and-after pair on a live machine is not evidence on its own: a DHCP lease renews, an adapter or a VPN transitions, a route is refreshed, policy pushes a proxy or firewall change — and a step that failed on any difference would be blaming the tool for the weather, while an ignore list wide enough to swallow all of it would swallow the finding too. So the step takes **two pairs**: a control pair with no run between the snapshots, and a treatment pair with the run between them — and the control's job is to say **whether the machine is quiet enough to measure at all**. A control pair that differs at all means the machine is moving on its own right now, and the step reports *not measurable* and stops: on a machine that drifts, no arrangement of snapshots can separate the tool from the weather, and a step that guessed would be worse than one that declines. With a quiet control behind it, a difference in the treatment pair is the step's finding — **and the item stops trying to make it a proof**. Drift that lands inside the treatment interval and persists is indistinguishable, by any arrangement of snapshots, from a change the run made: a lease renewed, a policy pushed a proxy, a VPN rewrote a route. Three review rounds went into trying to design that away and it cannot be designed away, so the item chooses instead: **a treatment-only difference fails the step**, drift included, and the cost of being wrong is a re-run and somebody looking. That trade is only affordable where the step runs — the acceptance machines, quiet by construction, not a CI runner — and it is the right way round for a step whose whole purpose is to catch a tool that started writing to the machine: a false alarm costs minutes, and a missed regression ships. So a candidate is not yet a failure — it makes the step **repeat both pairs**. But the repeat **classifies a candidate; it never absolves one**, and the difference matters for the very regressions this item exists to catch: a run that sets a registry value or disables an adapter changes the machine once, and the repeated pairs then start from the already-changed state and produce no second difference at all. A rule that passed on “it did not repeat” would let exactly that through. So the step keeps the **first snapshot of the session as a baseline** and compares the final state against it as well as pair-wise: a difference that repeats is a change the run makes every time; one that does not repeat but is still there against the baseline was made once and left — **or came from outside during that interval**, which the step reports as the two possibilities it cannot separate rather than choosing between them. The classification is guidance for whoever investigates, not a verdict the step reaches on its own, and nothing is passed silently: every difference is reported with both pairs kept. Acceptance: the two-pair step over `ipconfig /all`, `route print`, both proxies, the firewall profiles and the adapter states, failing on any difference the treatment pair shows once the control pair has come back clean, drift included; a negative self-test in which the harness itself makes a change between the treatment snapshots — a temporary route, removed afterwards — and the step fails as it must; and the step run on the acceptance machines rather than on a CI runner, whose network state is nobody's to reason about. — *raised while asking which rows of the user-manual walk could be automated, 2026-09-08; the walk does it by hand as row W54, which is exactly the work this item would stop repeating.*

### 43 — The tool does not say when the reports are being written into a synced folder

The report is written beside the program, and the person is told where it went; nobody tells them that the folder is being copied to a cloud service as they read it. Measured during the user-manual walk on `win11-enUS`, 2026-09-08: the package was extracted to the Desktop, which Windows 11 backs up to OneDrive by default, so `en-US\Reports\` was a synced folder and every report — computer and user names, MAC and IP addresses, SSID and BSSID, DNS servers, proxy settings, routes, driver versions, profile paths — went to the cloud as it was written. Nothing about that is the tool's doing, which is exactly why the tool is the only thing in a position to say it: it knows the path it resolved, at the moment it resolves it, and the person is standing in front of it. The documents can only warn in advance — the 1.2.5 fix does that in the user manual's sections 1, 5 and 9 and in the IT deployment manual's deployment section — and a warning in advance is read once, months before the run that matters.

What it would take. The report directory is already resolved and already reported (`Initialize-OutputDirectory`, the *Report Directory* field of *Computer and Run Information*, and the Startup Notice row that fires when the usual folder cannot be written). One more test at the same point: does the resolved path lie under a location known to be synced? OneDrive is the case that matters on a default Windows 11 machine and the one that can be detected honestly — `%OneDrive%`, `%OneDriveCommercial%` and `%OneDriveConsumer%`, which Known Folder Move sets and which a redirected Desktop or Documents path sits under — with a named, extensible list for the others (Dropbox, Google Drive, Box) rather than a guess at all of them.

**The rule that keeps it honest: the row may only ever be positive.** *This folder is inside <service>, so the reports are copied there* is a fact the tool can establish; *your reports stay on this computer* is not, because a mapped drive, a third-party client or a folder redirection nobody told the tool about can sync anything. A tool that says nothing when it detects nothing is telling the truth; a tool that promises local storage is guessing with the person's data.

It is an **Information** row, not a warning: nothing is broken, the verdict must not move, and a Startup Notice would push a healthy run to *Attention Required* for a configuration choice — which is closed item #39's mistake in a new place. Acceptance: on a machine where the report folder resolves under a detected sync root, one Information row naming the service and the folder, with the same fact beside the report path in *Computer and Run Information*; on a machine where nothing is detected, no row and no claim either way; the detection list extensible in one place with a reason recorded per entry; unit tests for a detected path, an undetected path and a path whose sync root is set but unrelated; the verdict unchanged in every case; the user manual's section 5 and the IT deployment manual's deployment section describing the row. — *raised by the owner during the user-manual walk, 2026-09-08, after finding 4 of that walk: the documents can warn, but only the tool can tell the person while they are looking at the path.*

**Status, 2026-09-09 — the owner's decision: this is the last of the tool items, not the next.** The reasoning, in the owner's terms: the copying is the computer's own behaviour and not the tool's, and the person in front of the machine knows better than the tool whether it syncs — somebody who set OneDrive up knows their Desktop is in it, while the tool can only infer it from a path. So the item stays open with its acceptance unchanged, and it is scheduled behind every other tool item. **What the decision does not undo:** the documents already warn in advance — 1.2.5's finding 4 rewrote the user manual's sections 1, 5 and 9 and the IT deployment manual's deployment section — the walk that found it stands in the record, and `tests/walk_capture.ps1` still refuses to write its own bundle into a synced folder, which is this same rule applied where the project does control the writing. **What was weighed against it**, kept here because this item argued it: a warning in advance is read once, months before the run that matters. The decision is that the advance warning is enough for now, and that a row on the screen is not worth the detection it would take.

### 48 — The walk asks a person to capture what a script could capture

The user-manual walk's sheet has an **Evidence** column, and eleven of its seventy-five rows name a `screenshot` there: the two shell dialogs, the extraction Explorer proposes, the window's title bar, what it shows while it runs and when it finishes, the report opening in a browser, and the IT panel with its *Reset to config*. Many more rows ask for no evidence and still turn on what the person saw — the window that starts by itself, the badges as they render, the counters under the verdict. A person produced all of it by hand on 2026-09-08, along with the file contents the other rows ask for — the launcher's error report, the three report formats, the resolved report path. Most of that is machinery a script already has in this repository: `gui_check.ps1` drives the real window through UI Automation in both languages and both entries, `launcher_check.ps1` starts the six launchers from staged copies, and the acceptance step runs the shipped console launcher and reads the JSON it produced. What none of them does is **keep what the person would have seen**, so the rows that turn on the wording of a dialog or on the way Chinese text lays out in a fixed-width panel are answered by a person retyping and re-describing, and the sheet's *Observed* column stayed empty because the answers went into the conversation instead (the walk's own entry in `VALIDATION.md` says so and asks the next walk to fix it one way or the other).

**What the harness would capture, and what it would not.** The rows it can answer are the ones whose evidence is a picture or a file: the shell's own dialogs and their exact words — the *Unblock* box, *Open File - Security Warning* with its publisher line, which are Windows' windows and not the tool's, so UI Automation must find and dismiss them rather than assume them; the tool's two windows at whatever size they resolve to on that desktop, with the display language's own strings in them, which is where a translated label that no longer fits is visible and nowhere else — `Initialize-Gui` asks for 940 wide and 700 or 872 high, then clamps the height to the working area, and the form scales with the display's DPI, so the real windows of the 120-DPI machine in this record are 1175 × 875 and 1175 × 1090; the harness records the size it was given and never sets one, because a label that fits at one size and not at another is the whole reason the picture is being kept; the launcher's error file, the report's three formats, and the resolved report directory with whether it sits under `%OneDrive%`. The rows it cannot answer are the six questions of the sheet's *Was it usable?* section — which its own introduction calls the part no assertion can measure — and they are the ones that produced #45, #46 and #47 — a screenshot judged afterwards by whoever wrote the sheet is a review of the manual, not a walk of it, and this project has the measurement that says the difference matters: the six findings of 2026-09-08 were found on a machine after five and eight rounds of review had passed the same sentences. The harness must therefore be written to **shorten** the walk, not to replace it: the person's time goes to those six questions and to whatever the harness marks as not matching the manual.

**Evidence comes back by hand.** The decision of 2026-09-08: the harness writes its artefacts and a manifest into one folder on the walked machine and stops there. Nothing is uploaded, no share is mounted and no network path is configured — a person copies the folder off the machine, the same way the reports of an acceptance run are carried today. **Where it writes matters as much as what it writes:** a folder made beside the walked package inherits that package's location, and the walk of 2026-09-08 ran from a Desktop that OneDrive backs up — so an evidence bundle written there, holding every screenshot and every report of the walk, would be in the cloud before anybody carried anything, which is finding 4 of that walk turned on the harness itself. The harness resolves the report directory's sync status already; it owes the same test to its own output, and must refuse a synced destination rather than quietly fill one. The harness ships in `tests/`, so it ships nothing to a user, and it must run on a machine that has only Windows PowerShell, like the rest of the package acceptance.

**The constraint that decides whether it works at all:** screen capture needs an interactive desktop session. A locked screen, or an RDP session that has been disconnected rather than logged off, yields black images from a GDI capture, and a harness that writes thirty black PNGs and reports success is worse than no harness. Capturing each window with `PrintWindow` instead of the screen covers the occluded case but not the locked one, so the state has to be detected and refused rather than captured.

Acceptance: one run of the harness on a machine that has not been walked answers, without further typing, every sheet row whose Evidence column asks for something — the shell dialogs as images with the tool's own windows at their real sizes, the launcher's error file and the report's three formats as files, and the resolved report path with its sync status as text — and answers it either with the artefact or with the words the sheet itself uses when a row cannot be reached on this machine: *not produced*, naming the precondition that was missing, because W40 needs a VPN, W47 an archiver that stock Windows does not have, W42 a machine without PowerShell, and W48 to W50 a failure nobody may manufacture. A row is answered either way and never silently absent. The manifest binds each answer to its row, so that the sheet's *Observed* column can be filled without the conversation that produced it; the harness refuses to run, naming the reason, when the session cannot be captured or when the folder it was given to write into is itself synced, and a bundle written to a synced folder is a failure of this item and not a detail of its use; the walk that follows spends the person on all six of the *Was it usable?* questions and on the rows the harness marks as unmatched; and the sheet says which rows the harness owns, so that a row is never answered twice. — *raised by the owner while planning the zh-TW walk, 2026-09-08.*

**Status, 2026-09-08 — the harness is in `tests/walk_capture.ps1`, and the walk it was written for has not happened yet.** One run answers all 51 rows it owns — 27 captured and 24 `not produced` on the reference machine — with `manifest.md` binding each answer to its row, and it refuses a session it cannot capture and a bundle destination inside a synced folder. It records the window at whatever size the desktop resolved (1175 × 875 here, against the 940 × 700 the form asks for), clicks the buttons the rows are about rather than computing what they should do — Run Again, Open Report Folder — and captures Windows' own dialogs by handle and by the file they name. Nine Codex rounds, 38 findings, all accepted, none rejected.

**Two paths in it are code without a measurement**, and they are written from the source rather than from a run, because this machine has produced neither: the partial-write path (the message box that names how many formats failed, its dismissal, and Open Report opening the survivor) and the emergency-file paths (a `NetworkHealthCheck_FATAL_*` file classified to W48 or W50 by the title inside it, with its own dialog). The first walk that meets either of them is what will say whether they work.

**What stays the person's**, and is marked so in the sheet's ownership table: the six *Was it usable?* questions, Explorer's Extract All wizard and compressed-folder view (W2, W3, W4), the browser's address bar (W13), the second unblocked extraction (W5b), the three numeric spinners of W34, the scenarios that need elevation or an ACL change (W43 to W46), and the rows that belong to other items (W36 and W37 to #41, W54 to #42).

The acceptance sentence above is met except for its last clause: *the walk that follows spends the person on the six questions and on the rows the harness marks as unmatched*. That clause needs a walk, and the zh-TW manual on a zh-TW machine is the one it is waiting for.

**Status, 2026-09-09 — the walk it was written for happened, and the harness answered 29 rows of it.** The zh-TW user-manual walk ran on `DESKTOP-CO7QIMR` with the released 1.2.5 asset: one run, **51 rows answered, 29 captured and 22 not produced**, `manifest.md` binding each answer to its row, the bundle carried off the machine by hand, and the person's time spent on the six *Was it usable?* questions — which produced #49 and a second instance of #46. The last clause of the acceptance sentence is met. **Three defects in the harness, all found by walking with it.** *It cannot see the dialog it opens:* `InvokeVerb('open')` blocks until the dialog is answered, so W5 was recorded as `not produced` while the security dialog stood on the screen — a false negative, and the worst kind, because the manifest cannot tell it apart from a precondition that was genuinely missing; the zh-TW dialog's own wording is therefore in the walk's record on the strength of a person reading it, with no picture beside it. *Its Cancel clicks do not dismiss a shell dialog*, the same defect from the other end. *Its console capture is stdout only*, so the two error lines of the launcher's mangled last line — [#50](#50--the-zh-tw-console-launchers-last-line-reaches-the-screen-as-a-command) — are absent from `W31-console.txt` and survive only in the screenshot taken beside it; that is [#47](#47--the-evidence-for-a-failure-that-writes-no-report-depends-on-the-person-retyping-what-they-saw)'s complaint about the launcher, reproduced inside this project's own test code. **Still unmeasured:** the partial-write path and the emergency-file paths, because nothing on this walk broke the report machinery either.

**Status, 2026-09-09 — the three defects its first walk found in it are fixed, and fixing the first one produced a picture the record was waiting for.** *The verb no longer blocks the thread that has to watch for the dialog.* `ShellExecute` does not return while the dialog it raised is on the screen, so the poll loop could not run until the dialog had been answered — which is how W5 came to be recorded as `not produced` with the security warning standing there. The open verb now runs on a background STA runspace (`Start-ShellVerb`), and the desktop is watched while the call is still outstanding; the row that could not be answered on 2026-09-09 is `captured` on the reference machine. *A dialog is closed and then looked at.* `Close-Dialog` tries Cancel through UI Automation, then a posted `BM_CLICK`, then `WM_CLOSE` on the dialog itself, checking `IsWindow` after each, and the row's note says which one worked or that the dialog is still on the screen. That measured something on its first run: **the security warning on Windows 11 26200 carries no control with `AutomationId` 2**, the button the old code clicked at, so the old code would have left it standing while its manifest said it had been cancelled — `WM_CLOSE`, which needs neither a button nor a language, is what closes it. The same helper now closes W1's property sheet, which had the same unverified click. *Standard error is kept.* The two redirected runs write it to `W31-console-stderr.txt` and `W28-launcher-window-stderr.txt`, kept only when there is something in them, and the row's note names the file or says the run printed nothing there — which is where [#50](#50--the-zh-tw-console-launchers-last-line-reaches-the-screen-as-a-command)'s two error lines would have landed instead of surviving in a screenshot by luck. **A fourth defect came out of running the fixed code**, and it is the reason the fixes were run rather than reasoned about: every property of a window that has been closed comes back empty, so the first repaired run named the dialog it had just captured `''`. The title is read while the dialog is still there.

**What this does not close.** The acceptance sentence above asks for a run on a machine that has not been walked, and these three fixes were measured on the reference machine: W5 `captured` with the dialog's own words in `W5-security-warning.txt`, a stderr file kept when a launcher wrote to it and deleted when it did not, and W28 and W29 unchanged. The next walk on a machine of its own is what closes the item. The two paths that are still code without a measurement — the partial-write dialog and the emergency-file classification — are still code without a measurement.

### 50 — The zh-TW console launcher's last line reaches the screen as a command

On `DESKTOP-CO7QIMR` (Windows 10 22H2, zh-TW display) the console launcher ended a **successful** run like this: 「網路健康檢查已完成。」, then `'◆ 代碼:0' is not recognized as an internal or external command,` and `operable program or batch file.`, then the pause prompt. The line that produced it is `echo 結束代碼：0` — `healthcheck/zh-TW/Start-NetworkCheck-Console.cmd:45`, the last thing the file prints before `pause` and `exit /b 0`.

**What is established.** The line immediately before it, `echo 網路健康檢查已完成。`, printed correctly on the same screen from the same file, and so did every Chinese echo of the launcher-error path on the same machine. The six launchers are UTF-8 without a BOM with CRLF endings, and the two zh-TW console-facing launchers set `chcp 65001` at their head. The en-US line is `echo Exit code: 0`, ASCII, and cannot fail this way. The same bytes, in a minimal batch file with the same `chcp` line, print correctly on the reference machine (Windows 11 26200, `ACP` and `OEMCP` both 950) and exit 0. The damage is confined to the screen: `exit /b 0` still runs, so the launcher's exit code and everything downstream of it are unaffected — what the person gets is an English error message in a Chinese window immediately after the sentence that says the check finished, which on the one screen that is supposed to reassure them reads as a failure.

**What is not established is the mechanism**, and it is worth saying plainly rather than guessing: the two machines differ in Windows build, in console host and possibly in codepage settings, and the failing line differs from its neighbour in containing an ASCII digit after full-width text. Any of those could carry it. The first work is therefore a measurement and not a change — a minimal batch file on the machine that produced it, then the same file with one variable moved at a time (the `chcp` line removed, a BOM added, the digit replaced, the full-width colon replaced) — because a fix chosen before the cause is known would be a guess that the next machine falsifies.

**The candidate fixes, so the measurement knows what it is choosing between.** Make the final line ASCII, as en-US's already is; drop it altogether, since the console has already printed 整體結果 and the three report paths; or fold the code into the completion sentence. All three are small; which is right depends on what the measurement says, and on whether any other Chinese echo in the six launchers shares the shape.

Acceptance: the failure is reproduced on the machine that produced it with a minimal file, and one variable is shown to carry it; a successful zh-TW console run then ends with no error text on the screen, on that machine and on the reference machine; `tests/launcher_check.ps1` covers the success path of the six launchers so the change cannot silently alter what a working run prints. — *found by the zh-TW user-manual walk, 2026-09-09; the harness's own console capture is stdout only, so the two error lines exist as evidence solely because the screenshot beside it caught them, which is [#48](#48--the-walk-asks-a-person-to-capture-what-a-script-could-capture)'s to fix.*

**Status, 2026-09-13 — untouched by the launcher change that closed #34, #44 and #47, and the measurement is prepared.** The six launchers were regenerated from one template for 1.2.14, and the two lines this item is about — `echo 網路健康檢查已完成。` and `echo 結束代碼：0` — are byte for byte what they were, with the same `chcp 65001` above them, so whatever carries the failure on `DESKTOP-CO7QIMR` is still there to be measured. The five minimal files the acceptance asks for are in `tests/probe-backlog-50/`, one variable each: the lines as shipped, the last line in ASCII, without `chcp`, with a BOM, the full-width colon replaced by an ASCII one; `run-all.cmd` runs the five with their output captured and records the machine's code pages, and each file is also to be double-clicked, because a redirected run may not take the path the console does. The item closes when that folder has been run on that machine and one variable is shown to carry it.

### 53 — The campaign self-test keeps no record of what the driver did, so its failures cannot be diagnosed

`tests/selftest_campaign.ps1` invokes `Invoke-AcceptanceCampaign.ps1` many times over — 45 of them through its `Invoke-Campaign` helper on the path CI runs, one more under `-Full`, and one that bypasses the helper — and captures each invocation's output, printing it back prefixed with `    | ` so that the chain's `campaign.log` carries it. It does not arrive. The log kept for run 236 of 2026-09-09 holds **two such lines in 199**, both from the one case that asserts on a message; every other invocation's output is absent, including the summary each one prints and the warnings the driver raises before it exits. What the log records is the self-test's own assertions: which one failed, and nothing about why.

**The cost is not hypothetical, and [#25](#25--two-campaign-refinements-m8s-refusal-message-and-the-bundle-names-resolution) is where it is being paid.** That item's bundle half has an explanation for the `campaign` step's intermittent failure — two invocations landing in the same second and colliding on `nhc-campaign_<computer>_<campaign>_<time>.zip`, which records a bundle failure and raises the exit code — and it has stood **unproven since 2026-09-06** for one reason, which the item states itself: *no bundle row was captured from the failing run*. The line that would have settled it, `bundle not written: …`, is one of the driver's own, and the driver's own lines are what the log does not keep. A second occurrence on 2026-09-09 added nothing to the diagnosis for the same reason, and its timing (Status on #25) now cuts against the explanation rather than supporting it — so the question is open, on evidence that was thrown away twice.

**And the evidence is thrown away on the machine that has it.** The chain uploads `campaign.log` as an artifact for exactly this purpose, with thirty days of retention; the runner has the driver's output in hand and writes 199 lines that do not include it. This is [#47](#47--the-evidence-for-a-failure-that-writes-no-report-depends-on-the-person-retyping-what-they-saw)'s complaint about the launcher — the evidence for a failure depending on someone happening to have seen it — reproduced inside this project's own test code, and the same shape [#48](#48--the-walk-asks-a-person-to-capture-what-a-script-could-capture) found in the walk harness, whose stdout-only capture is why [#50](#50--the-zh-tw-console-launchers-last-line-reaches-the-screen-as-a-command)'s two error lines survive in a screenshot rather than in a file. Three items now, in three different pieces of this project's machinery, and the same sentence covers all of them: *the thing that failed said something, and nothing kept it.*

**Where the lines are lost is not open, and this item's first draft was wrong to leave it open** (PR #38, round 6). `Invoke-Campaign` prints them with `Write-Output` (`:56`) and then returns its hashtable — and **every one of its callers assigns the call**, `$rN = Invoke-Campaign …`. PowerShell therefore collects the printed lines into `$rN` together with the hashtable instead of letting them reach the parent's stdout, and member-access enumeration keeps `$rN.ExitCode` answering correctly over that array, so nothing ever looked wrong. **The log proves it from the other side**: the only driver lines that survive are case 12's, and case 12 is the one place that calls the driver directly and prints `$out12` at script scope (`:205`) rather than through the function. The one call that does not go through `Invoke-Campaign` is the one whose output arrived. The chain is not involved — `Invoke-Native` writes every line it receives (`Invoke-ValidationChain.ps1:91`-`:95`), and it received these lines never. So the fix is in one file and one function: the prefixed lines go to the host rather than to the success stream the assignment swallows. **The acceptance below describes that set instead of sizing it**, because the first draft of this item guessed *some thirty* and the round-6 acceptance then hardened the guess into *all thirty of them* — a check written to that sentence would have stopped at thirty and left fifteen invocations unverified (PR #38, round 7). Four rounds of this review went on removing counts from this item before I put a fresh one into its acceptance.

Acceptance: `campaign.log` carries **every** invocation's driver output — every call through the helper and the one that bypasses it, under `-Full` as well as without it, counted from the script rather than from this sentence — ordered with the assertions that follow it, so that a failing case can be read afterwards and the reason named; a deliberate failure injected into one invocation — the bundle path held open, which case 24 already arranges — is diagnosable from the log alone by someone who did not run it; and the next occurrence of the `campaign` step's intermittent failure either confirms #25's collision explanation or replaces it, on what the log says rather than on elimination. — *raised by the owner on 2026-09-09, after a second occurrence of #25's failure taught nothing that the first had not.*

**Status, 2026-09-09 — the harness half is done and measured; the last clause waits for an occurrence.** `Invoke-Campaign` writes the driver's lines to the host instead of the success stream its callers assign away, and case 12's direct invocation writes them the same way, so every driver line in the file travels one path. Measured on the reference machine, the same run before and after: `campaign.log` goes from **199 lines carrying 2 of the driver's** to **1771 carrying 1562**. The ordering the acceptance asks for holds — case 20's block puts its driver output first and its two assertions last — and the deliberate failure case 24 injects now names itself: *bundle not written: The process cannot access the file 'campaign_summary.md' because it is being used by another process.* A reader who did not run it can say what happened, which is what the second clause asked for.

**Two guards, because a fix nothing holds is a fix that comes back, and both sit after the `-Full` block so that the optional campaign counts towards them.** **Case 35**, five assertions on this file's own AST and on its behaviour: `Invoke-Campaign` is defined once; it writes nothing to the success stream; it *still* forwards the captured lines to the host, which absence alone cannot establish; every one of the 46 calls to it is assigned, which is why the first rule exists; and every driver line the file captured reached the host, counted where each happens — the helper's and case 12's alike. **Case 36**, two assertions: the file has assertions to check, and every `Assert-True` call in it binds exactly its name, condition and detail. The second reports through an `if` over the counters rather than through `Assert-True`, because a call that reports argument-binding defects must not depend on argument binding.

**Mutation-tested, four ways, and one of the four had to be thrown away.** Deleting the helper's forwarding loop, and replacing it with `$out | ForEach-Object { … }` — an implicit success-stream emission with no `Write-Output` command name for a negative check to find — both leave the three original assertions passing and fail the two added ones. Deleting case 12's own loop fails the count with *captured 1562, forwarded 1560*. Reintroducing the quoting defect fails case 36 with *line 702: 6 elements*, while the mutated assertion itself passes vacuously with its name truncated at the apostrophe. **The fourth attempt at that last mutant was invalid and is recorded rather than discarded**: a single apostrophe leaves the quoting unbalanced, so the script threw before reaching the guard, and its red run proved nothing about the guard — a mutation that fails for the wrong reason is not evidence. The step is **132 / 132 in 105.3 s**.

**The defect this guard exists for was found in the guard itself.** Case 35's count assertion was first written with `the helper's and case 12's alike` inside a single-quoted name; PowerShell closed the string at the first apostrophe, bound the rest as positional arguments, and took the bareword `s` as the condition. It reported PASS against the mutant it had been written to catch, with the counters differing by two. An AST scan of the file found 135 `Assert-True` calls and exactly one malformed — that one; the 132 that predate this change were sound. Case 36 exists because of it.

**What is left is the third clause**, and it cannot be forced: the next time the `campaign` step fails on CI, the log will carry what the driver said, and that either confirms [#25](#25--two-campaign-refinements-m8s-refusal-message-and-the-bundle-names-resolution)'s collision explanation or replaces it. Until then this item stays open on a clause that waits for a machine, like [#48](#48--the-walk-asks-a-person-to-capture-what-a-script-could-capture)'s did.

### 55 — Nothing checks that this page's index matches its own items

The table at the top of this page is the index, and this file carries both it and the item bodies it points at — but nothing reads one against the other. [#54](#54--the-host-name-predicates-character-space-is-closed-by-example-rather-than-by-rule) reached `main` without its row: raised by PR #41's rounds 18 to 21, given a body of its own in `ef6317d` at 15:49, merged in `531b9b2` at 15:58, and still absent from the table, which ended at #53. Until the row was added in `4d50150` at 16:41, the one place that answers *what is still open?* said twenty-two where the file itself said twenty-three, and the repository `README.md`'s roadmap row, which counts from this table, said twenty-two as well. Fifty-two minutes rather than a release, and only because a person counted the headings that afternoon.

**Nothing in `tests/` reads this page.** `tests/doc_facts.ps1` reads the documents against the identifiers the two scripts define, and this page is not among them — it is a repository document, outside the package, like [`application-control.md`](application-control.md) beside it — and no other step of the chain opens it either. What the chain does have is the shape of the check applied to one number: `Invoke-ValidationChain.ps1` reads the total `tests/README.md` advertises and fails the run when it is not the total the run produced. Four such statements are available here, and every one of them is something this page already says about itself in *How to read an item* — that a number is permanent, never renumbered and never reused, and that a closed item keeps its number: an open row has an item body, an item body has an open row, no number stands in both tables, and no number between 1 and the highest used is missing from both.

This is [#41](#41--nothing-checks-that-the-packages-own-file-table-is-complete)'s shape one document further out: there, a file shipped in the package that no manual's file table names; here, an item with no row and a row with no item. Acceptance: a chain step over `docs/backlog.md` that fails on each of the four — an open row with no body, a body with no open row, a number in both tables, a number between 1 and the highest used in neither — with a self-test that mutates the page once per assertion and shows the step failing on that assertion alone; and the open count the repository `README.md` advertises checked against the index's row count in the same step, the way the chain already checks `tests/README.md`'s total, so the roadmap cannot drift from the table it summarises. — *raised by the change that added #54's missing row, 2026-09-10 (PR #42).*

**Status, 2026-09-11 — the drift has moved into the README's prose, where no count can see it.** This item was raised by a missing row, and its acceptance is built for that: an open row with no body, a body with no open row, a number in both tables, a gap in the numbering, and the repository `README.md`'s open count against the index's row count. Three things have gone wrong since, and **not one of them is a counting error.** PR #44's round 5 — *the README generalised one machine's measurement into a platform fact* — where the fix was to scope closed item #61's Wi-Fi statistics back to the one machine, one adapter and one driver actually measured. PR #46's round 6 — *the README kept broadcasting the conclusion round 3 withdrew from* closed item #63. And the change this line arrives in found the README's Backlog row and #39's own body both still saying that 1.2.8 was *merged and not yet released* after it had been: the release was published at 21:38 UTC on 2026-09-10, and the two changes that followed were both written and merged after it — PR #45 branched thirty-eight minutes after the release and merged three and a half hours after it, PR #46 branched three and a half hours after it and merged four hours and seven minutes after it — each carrying the sentence past the event that had falsified it, and the correction is this change, four hours and forty-nine minutes after the release — one stale sentence in two documents. **The four assertions above, and the open count beside them, would have caught none of the three.** One assertion is still worth adding, because that count is a single total which a pair of compensating errors satisfies: every `#N` the README's Backlog row names as open is an open row in this index and every open row's number is named there — the enumeration in both directions rather than the total — with the closed list equal to the closed table's numbers, and the category arithmetic it states summing to the open count plus one for each item that row says it counts in two groups — today #37 alone, whose halves are a measurement and a decision, so the groups sum to one more than the items. The totals themselves are deliberately not restated here: they moved twice while the change that wrote this line was open — thirty items became thirty-one when #64 was raised, and the groups became six when #64 needed a kind of its own — and a Status line that carries them would be the drift this item is about. The README states the overlap; the assertion is the arithmetic, not the totals. **The rest is prose and will stay prose, so it is recorded as a rule rather than as an assertion:** the README's row restates this page and is never a source for it, so a sentence there about item N has to be traceable to a sentence in item N's body, and a change that edits either reads both. Each of the three above was found by reading the two documents side by side — two of them by the review, the third by the change that closed the items — which is so far the only thing that has caught this class at all.

## Closed items

One line each: what it was, and the release or the date that closed it. The evidence is that release's entry in `healthcheck/VALIDATION.md`, which says what was changed and what proved it; the wording each item carried while it was open is in this repository's history.

| # | What it was | Closed |
|---|---|---|
| 1 | GUI fallback gap: an exception during form construction exited 1 instead of falling back to console mode | v1.1.3 (2026-07-29) |
| 2 | On a report-write failure the emergency report was produced twice — two dialogs in GUI mode | v1.1.5 (2026-09-03) |
| 3 | One failed report format was treated as a failure of the whole run: *Open Report* stayed disabled even when the other two were written | v1.1.5 (2026-09-03) |
| 4 | Retransmission thresholds parsed with raw `[double]` casts, packet-loss and latency thresholds rounding decimals silently | v1.1.4 (2026-09-03) |
| 5 | CIM fallback path: an IPv6 gateway could satisfy the IPv4 default-gateway check, and a null DHCP state was reported as static rather than unknown | v1.1.4 (2026-09-03) |
| 6 | Dead stores: `$script:NetworkSnapshot` and the local `$systemSummary` | v1.1.4 (2026-09-03) |
| 7 | A retransmission ratio above 100 % was left unexplained | v1.1.3 (inline ratio note) |
| 8 | The `AUTO_GATEWAY` placeholder leaked into a failure detail raw | v1.1.3 (explanatory detail) |
| 9 | An optional ping target's Information badge was left unexplained | v1.1.3 (inline explanation) |
| 10 | Virtual adapters earned Pass rows and inflated the adapter count; a zero-traffic adapter's "0 errors" is vacuous evidence | v1.2.0 (2026-09-03) |
| 11 | Full PowerShell stack traces, with local file paths, rendered in the HTML report | v1.1.4 (2026-09-03) |
| 12 | Each check's details needed a *Method* line | v1.1.1 (2026-07-28) |
| 13 | No Wi-Fi RF data (RSSI, channel, band) for wireless adapters | v1.2.0 (2026-09-03) |
| 14 | OS-locale exception strings passed through verbatim into reports of the other language | v1.2.2 (2026-09-04; the `Cause:` line, 23 codes in both languages) |
| 15 | GitHub's *Download ZIP* shipped LF line endings, so the auto-generated archive was not the validated package | 2026-09-03 (`.gitattributes`) |
| 16 | The test scripts lived outside the repository, in a session scratchpad | 2026-09-04 (PR #5: `tests/` with `Invoke-ValidationChain.ps1`) |
| 17 | The validator's two PowerShell guards were regular expressions over text, not the AST | 2026-09-04 (`tests/ast_guards.ps1` with `tests/selftest_guards.ps1`) |
| 18 | A restricted PowerShell language mode stopped the tool with an engine error nobody could act on | v1.2.2 (2026-09-04; the guard ahead of every other line, exit code 3, the environment report) |
| 19 | Every run was made on one machine — no acceptance anywhere else | 2026-09-06 (two VMs, 10 of 11 scenarios PASS on each; the one gap left became #31) |
| 20 | No continuous integration: nothing re-ran the chain automatically | 2026-09-05 (PR #9: `.github/workflows/validation-chain.yml`) |
| 23 | A refusal before the script's first line — a Group Policy execution policy, an application-control rule, an EDR — left nothing in plain language about what IT can do | v1.2.3 (2026-09-06) by #28, recognised 2026-09-08; the case it does not cover, where the launcher itself is refused, is #37 |
| 24 | The checklist of #19 was a list a person walked with the runner beside them | 2026-09-05 (PR #11: `tests/Invoke-AcceptanceCampaign.ps1` with `tests/selftest_campaign.ps1`; two refinements deferred to #25) |
| 26 | The compressed-folder detection knew one shape of the view folder — the Windows 10 one | v1.2.3 (2026-09-06) |
| 27 | A machine without a default route was reported as *the route table could not be read*, and the tool's own timeouts carried no cause | v1.2.3 (2026-09-06) |
| 28 | `LauncherError.txt` suggested extracting the ZIP whatever had stopped the launcher | v1.2.3 (2026-09-06) |
| 35 | *All checks passed* was said whatever the Information rows held, so an optional target that failed outright read as if nothing had | v1.2.7 (2026-09-09) |
| 36 | A stray value on the command line bound to `-ConfigPath`, so a second target was lost in silence and the run ended Test Incomplete | v1.2.7 (2026-09-09) |
| 39 | A measurement that could not be taken outranked a measurement that failed: one unreadable quality counter turned a run in which every connectivity check had passed into *Test Incomplete* | v1.2.8 (2026-09-10); the row was not moved until 2026-09-11, which is the drift #55 is about |
| 40 | The report explained the *Unable to Check* badge, and qualified a retransmission rate, whether or not the run had produced either | v1.2.7 (2026-09-09) |
| 45 | The IT panel checked a typed target only after the run had started, so a typing mistake cost a run and a verdict | v1.2.7 (2026-09-09) |
| 46 | The window named where the report was and never which file to send; the console named all three | v1.2.7 (2026-09-09) |
| 49 | The IT panel's longest label did not fit the box the code gave it, so the format it carried was in the source and not on the screen | v1.2.7 (2026-09-09) |
| 51 | A quality verdict could rest on a single packet: four echo requests made one lost reply 25 % against a 20 % critical threshold, and `MinimumTcpSegmentsForRate` was exactly `100 ÷ TcpRetransmissionWarningPercent`, so at the smallest sample the tool would rate, one retransmission was the warning threshold itself | v1.2.10 (2026-09-11) |
| 59 | No ping said which adapter it left by, so on a docked laptop the gateway row could not be placed | v1.2.9 (2026-09-11); the account the packaged guides give of what shipped is #64 |
| 63 | `TcpRetransmissionCriticalCount` warned on its own whatever the rate was, so a busy machine at 1.487 % — below both of the tool's own thresholds — reported *Attention Required* | v1.2.10 (2026-09-11); the burst signal its removal gives up is #65 |
| 66 | The adaptive ping extension had never run on a real machine: both validation chains of the 1.2.10 work took `complete` or `silent` every time, so everything that hangs off the third branch had only its unit cases | v1.2.10 (2026-09-11); the third chain entered it twice within the hour, once the reference machine's link began losing packets — 18 of 21 on two targets, two of the three timeouts inside the spread, and the step no slower than the same step in the two chains that lost nothing |
| 56 | The twelve shipped thresholds had no recorded basis, and the standards that exist do not supply one; decided 2026-09-10 to name every value in every threshold table as an operational default chosen without an external basis, the two policies apart from the magnitudes, changing no number | v1.2.11 (2026-09-11); the record the guides and the IT manual point at is `docs/thresholds.md` |
| 57 | The retransmission percentage divided by `Segments Sent`, which counts acknowledgements and excludes segments carrying only retransmitted bytes, so it was not the quantity any published figure refers to and no document said what it divided by; decided 2026-09-10 to keep the figure and state its denominator, the Win32 data-segment path refused because it needs the Administrators group and a write the machine keeps | v1.2.11 (2026-09-11) |
| 58 | The one required ping target is answered by the gateway's own stack, so its failure was ambiguous where the tool read it as a conclusion; decided 2026-09-10 that it stays required, with what its failure does and does not establish carried into the failed row, the *What to tell IT* summary and both manuals, in the field manual's words | v1.2.11 (2026-09-11) |
| 64 | The shipped guides stated the route selection as a rule for every ping row while the paragraph under it described the name target correctly, and named nowhere the three shapes of row that carry no route line | v1.2.11 (2026-09-11) |
| 32 | The report recorded the DHCP mode but not the server that answered the lease | v1.2.12 (2026-09-12) — landed as a line in each adapter row's own details, beside the mode it qualifies, rather than as the row per adapter the acceptance had asked for |
| 60 | There was no near-end baseline, so no measurement could be attributed to a segment: the ladder was cumulative, and no document said its rungs could not be subtracted | v1.2.12 (2026-09-12) |
| 67 | A gateway that answered slowly was reported under *Gateway does not answer*, because the fingerprint read a row's status and not its measurement | v1.2.12 (2026-09-12); decided 2026-09-12 that a slow-but-answering gateway is a quality fingerprint, not a connectivity one |
| 52 | The retransmission statistic cannot say whose traffic it measured | v1.2.12 (2026-09-12) — the attributable figure is, in each answering TCP target's own row, its `PingCount` handshakes with the SYNs each socket had to send again as the socket itself counted them (`SIO_TCP_INFO`), the times read against the operating system's initial retransmission timeout only where a socket cannot be asked; it decides nothing, by decision, and the system-wide rows point at it |
| 61 | On a Wi-Fi-only machine nothing distinguished the air from what is behind the access point: three signs were collected and none compared | v1.2.13 (2026-09-12) — the retry-counter half shipped in v1.2.12 (2026-09-12); the other half here: the access point sampled three times (`wifi-association`) with the roam out and back between two samples stated as invisible, the WLAN event log measured and not read because none of its events carries a BSSID and its re-association fires on a key rotation as well; the access-point-is-the-gateway comparison as a hint on the `gateway-neighbor` row; the spread of every ping row's replies with a stated minimum of nine, from the standard error of a standard deviation at a chosen quarter; the netsh parser's blocks split at the `GUID` label; the `sop/` Wi-Fi-only flow completed; six decisions taken 2026-09-12 |
| 62 | The Wi-Fi row inferred its connected state from the presence of the BSSID, so an unassociated radio and an associated one whose BSSID did not appear would have read as the same thing | v1.2.13 (2026-09-12) — measured on 2026-09-12 before it was designed: where desktop programs may not use the location, `netsh` withholds the whole interface block and exits 1, not one field, while the WLAN service still lists the interface and its state; the state now comes from the service, and the row names what `netsh` could not print and why |
| 65 | Nothing in a run could tell a burst of TCP retransmissions from an even spread — the counters were read once before the window and once after it — and #63's decision had removed the signal that fired on either | v1.2.14 (not yet released; the date follows the release page once it ships): the counters read again inside the window at least `RetransmissionIntervalSeconds` apart, one attempt per read and the reads stopped by the first that fails, the interval that held the most retransmissions named beside the whole-window figure with its share of the count and of the time; decided 2026-09-13 against the measured cost |
| 34 | The user launcher closed its window over a console-mode fallback, taking the report paths with it | v1.2.14 (not yet released; the date follows the release page once it ships): a fallback run waits for a key before the launcher's window closes, and only then — not with `-ConsoleOnly`, not without an interactive session, not with a redirected standard input |
| 44 | The launcher's own timestamp could not be read without knowing the machine's regional format | v1.2.14 (not yet released; the date follows the release page once it ships): the `Date/time:` line states the short-date pattern read from `HKCU\Control Panel\International`, omitted where it cannot be read, nothing converted; the file's own modified time named in both manuals as the authoritative stamp |
| 47 | The evidence for a failure that writes no report depended on the person retyping what they saw, and the error report overwrote itself | v1.2.14 (not yet released; the date follows the release page once it ships): PowerShell's error stream kept in `PowerShellMessages_<stamp>.txt`, printed back and named by the report and the suggestion; `LauncherError_<stamp>.txt` one per attempt; the six launchers regenerated from one template, the report written one line at a time so that a parenthesis in the folder's path cannot end the write |
| 54 | The host-name predicate's character space was closed by example rather than by rule | v1.2.14 (not yet released; the date follows the release page once it ships): the rule stated once in `Get-HostNameSyntaxProblem` — the name on the wire is the name configured, up to ASCII case and canonical composition; letters, digits, hyphen and underscore once encoded; an IPv6 literal with a numeric zone — and tested against an oracle over every code point of the Basic Multilingual Plane, which found on its first run that a case-insensitive match had folded the Kelvin sign and the dotted capital I into ASCII; the three candidates answered: zero-width characters refused, bidirectional controls refused, numeric zone ids accepted; the row names the character that decided a refusal |

## Adding and closing an item

**Adding.** An item is added when something is found that will not be fixed in the change that found it: a review finding deferred, a measurement that cannot be made on the machines at hand, a decision with a cost. Give it the next unused number, state what is wrong in the terms of the code or the machine that makes it wrong, name where the evidence is, and end with the acceptance sentence — what would have to be true for the item to be closed — and an origin note saying what raised it and when. A finding that can be fixed in the change that found it is not a backlog item; a wish with no acceptance sentence belongs in the roadmap table of the repository `README.md` instead.

**Closing.** An item is closed by the change that closes it, in the same commit that ships it: move its line to the closed table with the release or the date that closed it, and leave its number where every other document already cites it. The reason it is closed is proved in `healthcheck/VALIDATION.md`, in that release's entry — this page says only which release did it, so that the claim lives in one place. An item can also turn out to have been closed by a release that never named it: #23 by #28, two days later. Then its closed line names both dates — the release that did the work, and the day it was recognised — so that neither is read as the other, and whatever that release did not cover becomes an item of its own rather than a remainder nobody wrote down.

**Neither.** When an item has moved on without being closed — part of it done, its premise overtaken, a measurement arrived — add a dated **Status** line at the end of the item rather than rewriting it, so that what it said when it was raised stays readable beside what is true now.
