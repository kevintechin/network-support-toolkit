# Network Support Toolkit

[![Validation chain](https://github.com/kevintechin/network-support-toolkit/actions/workflows/validation-chain.yml/badge.svg)](https://github.com/kevintechin/network-support-toolkit/actions/workflows/validation-chain.yml)

A working support engineer's toolkit: a fault-isolation SOP and a portable network health-check tool.

## Components

### 📋 [sop/](sop/) — Network Troubleshooting SOP

A one-page fault-isolation playbook, from the user's chair to the ISP ([markdown](sop/network-troubleshooting-sop.md) · [one-page HTML](sop/network-troubleshooting-sop.html)):

- **Station 0 — Profile the Reporter**: IT-professional vs general-user delegation tracks
- **Front door**: four isolation questions that cut the search space before touching anything
- **The Packet's Journey**: eight stations, each in *read state → verify config → healthy → if broken* form
- **Read State Before Config**: tables / counters / probes, a counter reference, and two fault discriminators
- **Escalation — when, and with what**: four hand-off rules, the evidence package that travels with the hand-off, and how the topology sketch, the device logs and the config exports are collected
- **Fingerprint library**: fast symptom-to-suspect lookups

### 🔧 [healthcheck/](healthcheck/) — NetworkHealthCheck (Portable)

A read-only Windows diagnostic a non-technical user can run with **one double-click** — no install, no admin rights. Produces HTML / TXT / JSON reports that drop straight into the escalation package. Bilingual (en-US / zh-TW), config-driven expected-standards comparison, delta-based counter sampling.

See [healthcheck/docs](healthcheck/docs/) for the technical guide (design, validation, known limits). The validation chain that every release runs through is committed under [tests/](tests/) and runs from a fresh checkout with one command; its deterministic part also runs on GitHub Actions for every push and pull request.

## Quick start

**SOP** — open [sop/network-troubleshooting-sop.html](sop/network-troubleshooting-sop.html) in any browser, or read the [markdown version](sop/network-troubleshooting-sop.md).

**NetworkHealthCheck** — no installation, no admin rights, no prerequisites beyond stock Windows 10 / 11 (Windows PowerShell 5.1 and .NET Framework 4.x are built into the OS):

1. Download the latest **NetworkHealthCheck** ZIP from the [Releases page](https://github.com/kevintechin/network-support-toolkit/releases/latest) and extract it to a local folder (Code → Download ZIP also works — the package is then under `healthcheck/`)
2. Open the `en-US/` folder (or `zh-TW/` for Traditional Chinese)
3. Double-click **`Start-NetworkCheck.cmd`** — the GUI runs all checks automatically and writes HTML / TXT / JSON reports into a `Reports/` folder next to the script
4. If the GUI cannot start, **`Start-NetworkCheck-Console.cmd`** runs the same checks in text mode

**IT staff** — double-click **`Start-NetworkCheck-IT.cmd`** in the same folder: the tool opens with a run-options panel (extra targets, sample length, optional checks) and the HTML report opens with the IT diagnostics expanded. The same options work as switches in console mode, e.g. `-ConsoleOnly -PingTarget 10.0.0.1 -TcpTarget fileserver:445 -SampleSeconds 20 -ExpandDetails`. The IT deployment manual inside the package (`en-US/NetworkHealthCheck_IT_Deployment_Manual_en-US.html`) covers the configuration file key by key, deployment, package verification and what security policy does to the tool.

> If Windows flags the downloaded ZIP: right-click the ZIP → Properties → **Unblock**, then extract. Corporate policies (AppLocker / WDAC) may still block PowerShell — the user manual inside the package (`en-US/NetworkHealthCheck_User_Manual_en-US.html`, section 6) says what the person at the machine sees and what to send, the IT deployment manual (section 8) says what each policy does to the tool and what reaches IT, and [docs/application-control.md](docs/application-control.md) keeps the measured facts behind that section, which change faster than the package.

## Design principles

- **Follow the packet, not the device list**
- **Fix one, test one** — predict the result before you retest
- **Read state before config** — state cannot lie, and it's read-only
- **A failed ping is a suspect, not a conviction** — corroborate before acting

## Status & roadmap

| Item | Status |
|---|---|
| SOP | v1.2 — *Collecting the infrastructure evidence*: what the topology sketch, the device logs and the config exports have to contain, and that a configuration export's secrets come out before the file travels. v1.1 — the eight-station map, the two fault discriminators and the escalation package |
| NetworkHealthCheck | v1.2.13 — the other half of #61 and #62, merged and not yet released (the date follows the release page): on a Wi-Fi-only machine the report now reads the three air-side signs it had collected and compared over none, and none of the three decides anything. **The connected state from the WLAN service, not from the BSSID (#62)**: where desktop programs may not use the location — Windows 11 24H2 and later — `netsh wlan show interfaces` prints no interface and exits 1 (measured 2026-09-12 at the user level and at the device level), and the report used to call such a machine wired while its retry rows said connected; the interfaces and their state now come from the WLAN service through the retry reader's own P/Invoke type, the connection query's return code (error 5) together with the consent store's Deny on a build that gates the details is the witness of the refusal — error 5 alone is reported as access denied with the cause unidentified — the radio row reports the connected interface with the network name, access point, signal and rates marked not reported and the setting named where it was witnessed, the association rows say the samples could not be taken, the no-interface row lists what each reader returned instead of guessing at wired or radio off, and the chain's oracle reads its wireless facts from the adapters and connection profiles instead of netsh's text. `FUNCTION_COUNT` 128 → 135; unit 893 × 2 → 928 × 2, report-stage 233 × 2 → 244 × 2, result-set 123 → 132. Five decisions taken 2026-09-12; #62 closes. **The access point, sampled three times** (`wifi-association`, an IT-scope row per wireless interface): `netsh wlan show interfaces` before the first measurement, with the IT diagnostics and after the last, the BSSIDs compared — the same access point throughout, a roam under the same SSID (A then B then A reads as two roams back to the first), another network, or no BSSID reported, never called disconnected (#62) — with the limit on every row: sampled, not watched, so a roam out and back between two samples is invisible; the WLAN AutoConfig log was measured and not read, because none of its events carries a BSSID and its security re-association fires on a key rotation as well as on a roam. **Is the access point the gateway?** — a line on each `gateway-neighbor` row comparing the gateway's MAC with the BSSID of the wireless interface that supplied it: identical is one device and no wire to compare against, a difference only in the locally-administered bit or the last octet is probably one device, the same vendor prefix is one maker, different prefixes suggest two devices — every one of them a hint, not a topology claim. **The spread of the replies** — every ping row with two or more replies carries the sample standard deviation of its round-trip times, whole milliseconds and so the timer's resolution at millisecond latencies, with a stated minimum of nine replies (where the standard error of a standard deviation falls below a quarter of itself, the quarter a chosen precision on the [threshold page](docs/thresholds.md)) below which the figure describes those replies and not the link; no threshold, no verdict, no JSON field. The netsh parser splits interfaces at the `GUID`-labelled line now — the exposure PR #52's review closed in the test chain, closed in the tool. `FUNCTION_COUNT` 120 → 128; unit assertions 795 × 2 → 893 × 2, report-stage 223 × 2 → 233 × 2, result-set 102 → 123. Six decisions taken 2026-09-12; #61 closes. v1.2.12 — a batch of three changes that shipped together on 2026-09-12. **#60, the near-end rung:** `Tests.NearEndTarget` names a host on the users' own subnet that is not the gateway — empty as shipped, an IPv4 address by rule, placed before it is probed: the gateway is refused, an address off every primary subnet is reported as not probed and counts for nothing — and its row and the gateway row each say which segments their probes crossed, with the reading rule on the near-end row: a rung that passes clears what it crossed at that moment, the first that fails puts the problem beyond the last that passed, and two rungs' figures are never subtracted into a segment figure; the SOP carries the three-way comparison that needs a second machine, and nothing computes per-hop loss. **#67, a slow gateway titled as a silent one:** every result carries a `Rule` field — `loss` or `latency` on ping rows, additive under schema 2 — and the fingerprint reads it, so *Gateway does not answer* is selected only by a gateway row that lost its replies, a gateway that answered every probe slowly is a quality finding (decided 2026-09-12), and with a near-end host configured the summary's second line says which side of the gateway the fault is on, where the two rows share an adapter (`Path`). **#32, which server answered the lease:** each adapter row's details name its DHCP server from `Win32_NetworkAdapterConfiguration`, and say *static*, *unavailable* or *none recorded* where that is the truth — a line beside the mode rather than the row per adapter the item asked for, which the closed line records. **#61, the retry-counter half:** on a Wi-Fi-only machine the air's signature is the 802.11 retry — a frame sent again because no acknowledgement came back, absorbed as delay, visible in neither the TCP retransmission rate nor the ping loss figure — and the tool now reads it: `Checks.WifiRetryCounters` (on as shipped, off from the file only) has a few lines of P/Invoke compiled in memory read the Native Wifi API's per-PHY MAC counters before and after the run, and the report carries one weightless `wifi-retry` row per wireless interface — retries over frames transmitted plus abandoned, the multiple-retry subset listed and never added, the entry with the largest transmitted delta as the interface's figure (the reference machine's driver mirrors its totals into all six entries, so entries are never summed), the TCP rows' backwards-counter branch, and no threshold, because none has a stated basis; a reader that cannot be compiled, a WLAN service that does not answer or a machine without a wireless interface gets one row saying so. Five decisions taken 2026-09-12 and recorded on the backlog item; the other half of #61 stays open. **#52, whose traffic the retransmission statistic measured:** the system-wide counters could not say, so each TCP target that answers is now connected to `PingCount` times — the first connection decides the row as before, the rest go to the address it reached, one after another, stopping at the first that fails — and the row lists the handshake times and, per connection, how many times the socket had to send its SYN again, the socket's own count read through `SIO_TCP_INFO` without elevation on Windows 10 1703 / Server 2016 or later; where a socket cannot be asked the times stand in, read against the operating system's initial retransmission timeout (`Get-NetTCPSetting` where it reports one, 1000 ms assumed and named as assumed where it does not) and called the proxy they are: the tool's own SYNs, to one named target, at that moment, which the system-wide rows point at whenever a run took the sample; no verdict, no JSON field, no threshold — the timeout is Windows' own, its provenance and the socket counters' on the [threshold page](docs/thresholds.md) — and a target that never answers costs what it did. `FUNCTION_COUNT` 108 → 120 over the batch's three changes; unit assertions 587 × 2 → 795 × 2, report-stage 169 × 2 → 223 × 2. v1.2.11 — a documents release carrying two sentences in the tool, closing four items of the backlog that were all decided before it. **#56, what the shipped thresholds rest on:** nothing external — no standard, measurement or recorded decision stands behind any value, and the standards that exist define how to measure or set objectives for a named service class, never a general fault threshold — so the IT deployment manual's threshold table gains a *Basis* column naming each value as a chosen magnitude or, for the two warning deltas of 1, the policy *any at all is worth showing*; the technical guides' limitation 9 says why calibration is necessary and points at the repository's new [threshold page](docs/thresholds.md), which records what was examined with every citation's source and check date; no number changed. **#57, what the retransmission percentage divides by:** the `Segments Sent/sec` counter — every segment this computer sent, acknowledgements included, segments carrying only retransmitted bytes excluded — so the figure is the tool's own and not comparable with a published retransmission rate; the row's own details, both manuals and the guides now say so, a unit test pins the arithmetic to the sentence, and no data-segment rate is computed because the counters that would give one are off until a member of the Administrators group turns them on. **#58, the one required ping target being answered by the gateway's own stack:** it stays required, and a failed gateway row, the *What to tell IT* summary and both manuals' reading guidance now say what the failure does and does not establish, in the field manual's words — a suspect, not a conviction. **#64, the guides' route-selection paragraph:** it stated its rule for every ping row while the paragraph under it described the name target correctly; it now states the rule for an address target, hands the name to the next paragraph, and names the three shapes of row that carry no route line. `FUNCTION_COUNT` stays 108; unit assertions 564 × 2 → **587 × 2**, report-stage 165 × 2 → **169 × 2**. Round 2 of its review raised open item #67 — a gateway that answers slowly is titled *Gateway does not answer*, which predates this release — and taught the failed gateway row to say which of the two happened. v1.2.10 — two items of the backlog, and they had to ship together because both change the same classification. **#51, a quality verdict resting on a single packet:** four echo requests made one lost reply 25 % against a 20 % critical threshold, and `MinimumTcpSegmentsForRate` was exactly `100 ÷ TcpRetransmissionWarningPercent`, so at the smallest sample the tool would rate, one retransmission was the warning threshold itself — the guard against rating too small a sample permitting the least informative case it could. `PingCount` becomes a **starting** count: every reply arriving ends the sample, nothing answering ends it too (100 % loss is conclusive at four probes, and that is the one case where every extra probe costs a whole timeout), and only "some lost but not all" continues — to the smallest count at which one lost reply is below the warning percentage **as the row prints it** — the figure is rounded to one decimal and the classification reads the printed one, so twenty-one at the shipped five and twenty-two at a calibrated 4.8 %, where the exact arithmetic would have stopped at twenty-one and left one packet deciding — or to the new `PingCountMaximum` where that is lower. Those probes are **spread across the rest of the run** rather than sent back to back, in the seconds the retransmission window was going to spend sleeping, so a healthy run is no slower and the extra samples span the run instead of one instant of it; the row is written where it belongs and rewritten when the sample finishes, so the ping section is never left in two pieces. A verdict is withheld only where **both** halves of the defect are present — a sample too coarse for the threshold **and** a classification that changes on one packet — so three lost of four still fails a required target at 75 % while one lost of four reports its numbers and decides nothing. On the retransmission side a count floor, `MinimumTcpRetransmissionsForVerdict`, gates **both** branches, because a floor on the warning branch alone would leave three retransmissions in fifty segments failing on the rate; it suppresses nothing above two hundred sent segments. And the window is extended once where a sample ends below the rating floor with a retransmission in it — the only case waiting longer settles. **#63, a count deciding the verdict without reference to how much was sent:** `TcpRetransmissionCriticalCount` warned on its own whatever the rate was, so 57 retransmissions of 3 832 segments — 1.487 %, below both of the tool's own thresholds — read *Attention Required*. The standalone warning goes and the escalation to Fail stays, so such a row now passes and a run whose only weighted warning was that row reports *Overall Healthy*; what it gives up is the only signal that fired on a burst inside an otherwise healthy window, which is open item #65. Every classified retransmission row now names the rule that decided it. The IT panel's ping spinners opened at 20 until this release, a number with no reason anywhere in the repository or the package while the configuration validated no upper bound at all; the range is now the configured ceiling, and v1.2.1's widening of it by a larger configured value is unchanged. v1.2.9 — one item of the backlog. #59, which adapter a measurement left by: every ping row whose probes ran now carries a route line in its details — the source address and the interface the route table selects for that target, read with `Find-NetRoute` — worded as a selection rather than as the path the echoes took, because the probes are sent unbound. A row where nothing was sent says that instead and has no measurement to place: a configured value that could never become a request, or a placeholder that expands to nothing on this machine — `AUTO_GATEWAY` where there is no default route, `AUTO_DNS` where the primary adapters list no DNS server. **A target given as an address is looked up before and after its probes**, and when the two lookups disagree the row says so and says that it cannot tell which of them carried the probes; **a target given as a name has no address to ask the route table about until something replies**, so it is looked up afterwards, once per address its replies came from, and the row says that no before-and-after pair was taken — the tool does not resolve the name itself, which would put the ping check behind the resolver on a machine whose resolver is often what is being diagnosed. Where those addresses do not all route through one interface, or their lookups do not all answer, the line says the measurement cannot be attributed to a single adapter; and the cmdlet missing, the route table returning nothing, a lookup that threw and a name whose probes got no reply at all are four reasons that each say which one they are, the row reporting its loss and its latency either way. It adds no JSON field — the sentence is a line in the row's existing details — and no check, threshold or sampling window moved. The chain run that validated it failed four real-window cases on a threshold rather than on the change, which is how #63 was raised. v1.2.8 — two items of the backlog. #38, a measurement the tool lost and the seconds it never accounted for: a performance-counter read that runs out of its eight seconds is attempted a second time, and only a second failure writes *Unable to Check* (that row keeps its status, name and message, and gains the attempts behind it); the baseline takes one throwaway read per class before the sample window opens, so the provider's first query is paid for outside the measurement; every attempt that failed is kept with the seconds it spent, even where a later one succeeded; each row's sample duration comes from its own protocol's two stamps rather than from the enclosing snapshots' — 13.3 seconds against a configured 8, on `win11-enUS`, was TCPv4's timeout inside TCPv6's window — and stands beside the configured minimum; and a window whose seconds went on counter reads that **failed** inside it says so, and names them - a read that was merely slow, or the checks that run between the two samples, lengthen a window without being itemised, and the duration printed is the true one either way. The failure rate itself is a property of a machine under load: `tests/tcp_counter_rate.ps1` measures it per condition, on demand, and never gates a build. #39, what the overall result is allowed to rest on: the verdict comes from what the run measured, so a row that could not measure, a sample too coarse for the threshold applied to it and a fact about this run's own input keep their badge, their place in the counts and their sentence in the summary without deciding the result — a `Weightless` marking on the row, additive under schema 2, read by the overall status and by every fingerprint predicate but by neither the counts nor the report's notices. Everything measured keeps its weight, optional targets included; and a configured target that could never become a request is refused before anything is sent, by one rule (`Test-HostNameSyntax`) that the run, the configuration validation and the IT panel's pre-check all ask, so a packet that never left can no longer be recorded as a measurement that failed. It reverses what 1.2.5 decided about the small sample: the same numbers are reported, and the verdict is left to what was measured. v1.2.7 — the first tool release since 1.2.3, and six items of the backlog about what the screen says at the moment it says it: the IT panel's labels carry their own widths so the clipped one fits and the headless GUI step measures every control against its box (#49); the panel checks a typed extra TCP target before the run starts, with the rule the run uses, and every free-text field shows an example (#45); the window and the console name one file to send (#46); the summary claims what the verdict made and names an optional target that did not answer (#35); the report explains the Unable to Check badge only when a row carries it and the retransmission rate only when one was computed (#40); and a stray value on the command line is refused by PowerShell instead of binding to `-ConfigPath` (#36). v1.2.6 — documents release: the two findings of the zh-TW walk of the user manual on a zh-TW machine, both of them the manuals' (the publisher line the Traditional Chinese security dialog actually shows, and all four manuals naming the IT panel's extra-TCP field by a label the panel clips — the format now stands in the text with an example value, and the panel itself is backlog #49). v1.2.5 — documents release: the six findings of the 1.2.4 user-manual walk on a real machine, all of them the manuals' (the tool's own eight-second limit on the retransmission counter read; the three small-sample messages, one of which moves the verdict; the field the launcher's error report also carries; the report copied to the cloud when the package sits in a folder OneDrive backs up, which the manuals promised the other way round; the folder inside the extracted folder; and what the unsigned-publisher dialog actually says). v1.2.4 — documents release: the end-user manual and the IT deployment manual ship inside the package in both languages (markdown and one HTML page each), the user READMEs are retired into them, and the validator checks every document's version labels. v1.2.3 — one run for everyone: "What to tell IT" summary, collapsed IT diagnostics (Wi-Fi radio, routes, gateway ARP, proxy, traceroute, drivers), physical/virtual adapter classification, JSON schema 2 with run profile and fingerprint, IT entry point (launcher + switches + options panel); v1.2.1 fixes the IT panel truncating a configured ping count / sample length above its default range (Codex round 7 on PR #3) and two 1.2.0 GUI regressions caught by the first real GUI runs (both entries fell back to console mode; the IT launcher opened the user layout); v1.2.2 closes backlog #14 (network errors classified by their error code, so the cause is written in the report's language and the operating system's own message is kept beneath it) and #18 (a guard ahead of every other line detects a restricted PowerShell language mode — what WDAC / AppLocker application control creates — and writes an environment report naming what IT can do, instead of an engine error nobody can act on); v1.2.3 closes backlog #26/#27/#28, the findings of the first acceptance campaign (the Windows 11 compressed-folder view recognized, a machine without a default route reported as such and the tool's own timeouts classified with a cause, LauncherError.txt suggesting the action that fits the reason); v1.1.5 closed backlog #2/#3, v1.1.4 closed #4/#5/#6/#11; self-explaining Method + Manual-check lines in every check; validation chain: static checks, independent code review, Windows acceptance, real-window GUI runs, five fault-injection scenarios ([validation record](healthcheck/VALIDATION.md) · [scenario matrix](healthcheck/validation-matrix.html) · [tests/](tests/) runs the chain from a checkout, backlog #16, holds the two PowerShell guards on the AST since backlog #17, and its deterministic part runs on GitHub Actions for every push and pull request since backlog #20; `tests/Invoke-PackageAcceptance.ps1` accepts a released asset on a machine with Windows PowerShell alone, and `tests/Invoke-AcceptanceCampaign.ps1` guides a person through every scenario of the checklist on such a machine, for the runs on other machines of backlog #19, now closed: a Windows 11 Home (en-GB) VM accepted 1.2.2 on 2026-09-05 and a Windows 10 Pro VM with a zh-TW system and an en-US display language accepted 1.2.3 on 2026-09-06, 10 of 11 scenarios PASS on each — the eleventh needs AppLocker to actually refuse a script, which neither machine achieved) |
| NHC v1.2 | Phase A shipped as v1.2.0 (no separate modes, IT entry point, IT diagnostics, schema 2 — [design](docs/design-v1.2-triage-wizard.md)); Phase C (report links to the wizard) follows the wizard |
| Guided triage wizard | Planned — single-file HTML decision tree over the SOP that imports the NHC JSON report, asks only what the machine cannot see, and produces the escalation package ([design](docs/design-v1.2-triage-wizard.md)) |
| Backlog | 21 items open — [docs/backlog.md](docs/backlog.md): seven changes to the tool (#34 the launcher closing its window over a console-mode fallback, #38 the retransmission counter read failing intermittently, three times in three runs made in a freshly extracted copy, whose one remaining clause since 1.2.8 — the read is attempted a second time now — waits for a machine that reproduces the failure, #43 nothing telling the person that the reports are being written into a synced folder - the last of the tool items by decision, since the copying is the computer's own behaviour and the person knows their machine better than the tool does, #44 the launcher's timestamp in the machine's own date format, #47 the launcher not capturing the message PowerShell printed, #50 the zh-TW console launcher's last line reaching the screen as a command after a successful run, #65 nothing in a run being able to tell a burst from an even spread, because the counters are read once before the window and once after it, so fifty retransmissions inside ten seconds and fifty spread across two minutes are the same two numbers — the count branch v1.2.10 removed for closed item #63 did fire on the first of those and could not name it, which is the signal that removal gave up and this item records, against a sub-window read whose cost has to be measured rather than assumed in either direction, #38 having measured a single counter read running out of its eight seconds), ten in `tests/` and the build (#21 a reproducible asset, #25 the campaign's `M8` message and bundle name, #29 the campaign's manual steps, #30 the Mark of the Web on a CI artifact, #33 tiers 2 and 3 of the document-fact step, #41 the file table's reverse direction, #42 nothing proving the tool changes nothing, #48 the walk asking a person to capture what a script could capture, whose harness took its first walk on 2026-09-09 and found three defects in itself, #53 the campaign self-test keeping no record of what the driver did, which is why #25's bundle explanation is still unproven after a second occurrence, #55 nothing checking this index against the item bodies it points at, which is how #54 reached `main` with a body and no row), one that is both (#54 the host-name predicate's character space, closed one example at a time over four consecutive rounds of PR #41's review and still not closed by a rule), two that wait on a machine nobody has yet (#31 AppLocker actually enforcing, and the measurement half of #37, a launcher refused before it runs) and two decisions (#22 signing, and #37's other half — what the package carries for that person) — #37 stands in two of those groups because its halves are different kinds of work, a measurement and a decision, so the groups sum to twenty-two where the items are twenty-one. Numbers 1 to 20, 23, 24, 26 to 28, 32, 35, 36, 39, 40, 45, 46, 49, 51, 52, 56 to 64, 66 and 67 are closed |

## Working method

Built and maintained **with AI assistance** — the methodology, requirements, and field experience behind it are mine; every release is validated end-to-end before it ships (PowerShell 5.1 parser checks, dual-language parity, unit and functional tests, real-window GUI runs, real-machine acceptance runs, fault-injection scenarios). See the [validation record](healthcheck/VALIDATION.md); the chain itself is in [tests/](tests/).

## Author

Kevin (Te-Chin) Lin · 2026
