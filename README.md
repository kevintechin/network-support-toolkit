# Network Support Toolkit

[![Validation chain](https://github.com/kevintechin/network-support-toolkit/actions/workflows/validation-chain.yml/badge.svg)](https://github.com/kevintechin/network-support-toolkit/actions/workflows/validation-chain.yml)

A working support engineer's toolkit: a fault-isolation SOP and a portable network health-check tool.

## Components

### 📋 [sop/](sop/) — Network Troubleshooting SOP

A one-page fault-isolation playbook, from the user's chair to the ISP ([markdown](sop/network-troubleshooting-sop.md) · [one-page HTML](sop/network-troubleshooting-sop.html)):

- **Station 0 — Profile the Reporter**: IT-professional vs general-user delegation tracks
- **Front door**: four isolation questions that cut the search space before touching anything
- **The Packet's Journey**: eight stations, each in *read state → verify config → healthy → if broken* form
- **Read State Before Config**: tables / counters / probes, a counter reference, and two fault discriminators
- **Escalation — when, and with what**: four hand-off rules, the evidence package that travels with the hand-off, and how the topology sketch, the device logs and the config exports are collected
- **Fingerprint library**: fast symptom-to-suspect lookups

### 🔧 [healthcheck/](healthcheck/) — NetworkHealthCheck (Portable)

A read-only Windows diagnostic a non-technical user can run with **one double-click** — no install, no admin rights. Produces HTML / TXT / JSON reports that drop straight into the escalation package. Bilingual (en-US / zh-TW), config-driven expected-standards comparison, delta-based counter sampling.

See [healthcheck/docs](healthcheck/docs/) for the technical guide (design, validation, known limits). The validation chain that every release runs through is committed under [tests/](tests/) and runs from a fresh checkout with one command; its deterministic part also runs on GitHub Actions for every push and pull request.

## Quick start

**SOP** — open [sop/network-troubleshooting-sop.html](sop/network-troubleshooting-sop.html) in any browser, or read the [markdown version](sop/network-troubleshooting-sop.md).

**NetworkHealthCheck** — no installation, no admin rights, no prerequisites beyond stock Windows 10 / 11 (Windows PowerShell 5.1 and .NET Framework 4.x are built into the OS):

1. Download the latest **NetworkHealthCheck** ZIP from the [Releases page](https://github.com/kevintechin/network-support-toolkit/releases/latest) and extract it to a local folder (Code → Download ZIP also works — the package is then under `healthcheck/`)
2. Open the `en-US/` folder (or `zh-TW/` for Traditional Chinese)
3. Double-click **`Start-NetworkCheck.cmd`** — the GUI runs all checks automatically and writes HTML / TXT / JSON reports into a `Reports/` folder next to the script
4. If the GUI cannot start, **`Start-NetworkCheck-Console.cmd`** runs the same checks in text mode

**IT staff** — double-click **`Start-NetworkCheck-IT.cmd`** in the same folder: the tool opens with a run-options panel (extra targets, sample length, optional checks) and the HTML report opens with the IT diagnostics expanded. The same options work as switches in console mode, e.g. `-ConsoleOnly -PingTarget 10.0.0.1 -TcpTarget fileserver:445 -SampleSeconds 20 -ExpandDetails`. The IT deployment manual inside the package (`en-US/NetworkHealthCheck_IT_Deployment_Manual_en-US.html`) covers the configuration file key by key, deployment, package verification and what security policy does to the tool.

> If Windows flags the downloaded ZIP: right-click the ZIP → Properties → **Unblock**, then extract. Corporate policies (AppLocker / WDAC) may still block PowerShell — the user manual inside the package (`en-US/NetworkHealthCheck_User_Manual_en-US.html`, section 6) says what the person at the machine sees and what to send, the IT deployment manual (section 8) says what each policy does to the tool and what reaches IT, and [docs/application-control.md](docs/application-control.md) keeps the measured facts behind that section, which change faster than the package.

## Design principles

- **Follow the packet, not the device list**
- **Fix one, test one** — predict the result before you retest
- **Read state before config** — state cannot lie, and it's read-only
- **A failed ping is a suspect, not a conviction** — corroborate before acting

## Status & roadmap

| Item | Status |
|---|---|
| SOP | v1.2 — *Collecting the infrastructure evidence*: what the topology sketch, the device logs and the config exports have to contain, and that a configuration export's secrets come out before the file travels. v1.1 — the eight-station map, the two fault discriminators and the escalation package |
| NetworkHealthCheck | v1.2.8 — backlog #38, a measurement the tool lost and the seconds it never accounted for: a performance-counter read that runs out of its eight seconds is attempted a second time, and only a second failure writes *Unable to Check* (that row keeps its status, name and message, and gains the attempts behind it); the baseline takes one throwaway read per class before the sample window opens, so the provider's first query is paid for outside the measurement; every attempt that failed is kept with the seconds it spent, even where a later one succeeded; each row's sample duration comes from its own protocol's two stamps rather than from the enclosing snapshots' — 13.3 seconds against a configured 8, on `win11-enUS`, was TCPv4's timeout inside TCPv6's window — and stands beside the configured minimum; and a window that ran long names the reads that lengthened it. The failure rate itself is a property of a machine under load: `tests/tcp_counter_rate.ps1` measures it per condition, on demand, and never gates a build. v1.2.7 — the first tool release since 1.2.3, and six items of the backlog about what the screen says at the moment it says it: the IT panel's labels carry their own widths so the clipped one fits and the headless GUI step measures every control against its box (#49); the panel checks a typed extra TCP target before the run starts, with the rule the run uses, and every free-text field shows an example (#45); the window and the console name one file to send (#46); the summary claims what the verdict made and names an optional target that did not answer (#35); the report explains the Unable to Check badge only when a row carries it and the retransmission rate only when one was computed (#40); and a stray value on the command line is refused by PowerShell instead of binding to `-ConfigPath` (#36). v1.2.6 — documents release: the two findings of the zh-TW walk of the user manual on a zh-TW machine, both of them the manuals' (the publisher line the Traditional Chinese security dialog actually shows, and all four manuals naming the IT panel's extra-TCP field by a label the panel clips — the format now stands in the text with an example value, and the panel itself is backlog #49). v1.2.5 — documents release: the six findings of the 1.2.4 user-manual walk on a real machine, all of them the manuals' (the tool's own eight-second limit on the retransmission counter read; the three small-sample messages, one of which moves the verdict; the field the launcher's error report also carries; the report copied to the cloud when the package sits in a folder OneDrive backs up, which the manuals promised the other way round; the folder inside the extracted folder; and what the unsigned-publisher dialog actually says). v1.2.4 — documents release: the end-user manual and the IT deployment manual ship inside the package in both languages (markdown and one HTML page each), the user READMEs are retired into them, and the validator checks every document's version labels. v1.2.3 — one run for everyone: "What to tell IT" summary, collapsed IT diagnostics (Wi-Fi radio, routes, gateway ARP, proxy, traceroute, drivers), physical/virtual adapter classification, JSON schema 2 with run profile and fingerprint, IT entry point (launcher + switches + options panel); v1.2.1 fixes the IT panel truncating a configured ping count / sample length above its default range (Codex round 7 on PR #3) and two 1.2.0 GUI regressions caught by the first real GUI runs (both entries fell back to console mode; the IT launcher opened the user layout); v1.2.2 closes backlog #14 (network errors classified by their error code, so the cause is written in the report's language and the operating system's own message is kept beneath it) and #18 (a guard ahead of every other line detects a restricted PowerShell language mode — what WDAC / AppLocker application control creates — and writes an environment report naming what IT can do, instead of an engine error nobody can act on); v1.2.3 closes backlog #26/#27/#28, the findings of the first acceptance campaign (the Windows 11 compressed-folder view recognized, a machine without a default route reported as such and the tool's own timeouts classified with a cause, LauncherError.txt suggesting the action that fits the reason); v1.1.5 closed backlog #2/#3, v1.1.4 closed #4/#5/#6/#11; self-explaining Method + Manual-check lines in every check; validation chain: static checks, independent code review, Windows acceptance, real-window GUI runs, five fault-injection scenarios ([validation record](healthcheck/VALIDATION.md) · [scenario matrix](healthcheck/validation-matrix.html) · [tests/](tests/) runs the chain from a checkout, backlog #16, holds the two PowerShell guards on the AST since backlog #17, and its deterministic part runs on GitHub Actions for every push and pull request since backlog #20; `tests/Invoke-PackageAcceptance.ps1` accepts a released asset on a machine with Windows PowerShell alone, and `tests/Invoke-AcceptanceCampaign.ps1` guides a person through every scenario of the checklist on such a machine, for the runs on other machines of backlog #19, now closed: a Windows 11 Home (en-GB) VM accepted 1.2.2 on 2026-09-05 and a Windows 10 Pro VM with a zh-TW system and an en-US display language accepted 1.2.3 on 2026-09-06, 10 of 11 scenarios PASS on each — the eleventh needs AppLocker to actually refuse a script, which neither machine achieved) |
| NHC v1.2 | Phase A shipped as v1.2.0 (no separate modes, IT entry point, IT diagnostics, schema 2 — [design](docs/design-v1.2-triage-wizard.md)); Phase C (report links to the wizard) follows the wizard |
| Guided triage wizard | Planned — single-file HTML decision tree over the SOP that imports the NHC JSON report, asks only what the machine cannot see, and produces the escalation package ([design](docs/design-v1.2-triage-wizard.md)) |
| Backlog | 22 items open — [docs/backlog.md](docs/backlog.md): ten changes to the tool (#32 the DHCP server, #34 the launcher closing its window over a console-mode fallback, #38 the retransmission counter read failing intermittently, three times in three runs made in a freshly extracted copy, #39 the verdict decided on 2026-09-09 - the overall result comes from what the run measured, so a quality measurement that could not be taken, a sample too coarse for its threshold and a fact about this run's own input all report without changing it, while every measured row keeps its weight, optional targets included, and the un-measured target leaves a row where its result would have been, #43 nothing telling the person that the reports are being written into a synced folder - the last of the tool items by decision, since the copying is the computer's own behaviour and the person knows their machine better than the tool does, #44 the launcher's timestamp in the machine's own date format, #47 the launcher not capturing the message PowerShell printed, #50 the zh-TW console launcher's last line reaching the screen as a command after a successful run, #51 a quality verdict resting on a single packet because the threshold is finer than the sample, whose prescription became adaptive sampling on 2026-09-09 rather than a larger fixed count, #52 the retransmission statistic being system-wide, so a small sample cannot say whose traffic it measured), nine in `tests/` and the build (#21 a reproducible asset, #25 the campaign's `M8` message and bundle name, #29 the campaign's manual steps, #30 the Mark of the Web on a CI artifact, #33 tiers 2 and 3 of the document-fact step, #41 the file table's reverse direction, #42 nothing proving the tool changes nothing, #48 the walk asking a person to capture what a script could capture, whose harness took its first walk on 2026-09-09 and found three defects in itself, #53 the campaign self-test keeping no record of what the driver did, which is why #25's bundle explanation is still unproven after a second occurrence), two that wait on a machine nobody has yet (#31 AppLocker actually enforcing, and the measurement half of #37, a launcher refused before it runs) and two decisions (#22 signing and #37's other half — what the package carries for that person), #39 having been decided on 2026-09-09 so that what is left of it is a change to the tool. Numbers 1 to 20, 23, 24, 26 to 28, 35, 36, 40, 45, 46 and 49 are closed |

## Working method

Built and maintained **with AI assistance** — the methodology, requirements, and field experience behind it are mine; every release is validated end-to-end before it ships (PowerShell 5.1 parser checks, dual-language parity, unit and functional tests, real-window GUI runs, real-machine acceptance runs, fault-injection scenarios). See the [validation record](healthcheck/VALIDATION.md); the chain itself is in [tests/](tests/).

## Author

Kevin (Te-Chin) Lin · 2026
